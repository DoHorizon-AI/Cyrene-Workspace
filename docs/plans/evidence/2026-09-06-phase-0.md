# Phase 0 evidence / Phase 0 验收证据

Recorded: 2026-09-06.

Status: 30/32 scoped Phase 0 checks have Actual PASS evidence in the records below;
P0-12, P0-29 and P0-GATE remain pending. This report supplements the 2026-09-05
report and does not replace or erase its earlier failures and limitations.

状态：下列记录支持 32 个 Phase 0 单项检查中的 30 个 Actual PASS；P0-12、P0-29 和
P0-GATE 仍未通过。本报告补充 [2026-09-05 历史证据](2026-09-05-phase-0.md)，不覆盖其中
保留的失败和限制。

## Scope and evidence rules / 范围与证据规则

The checked items are component and real-process proofs. A checked item has a
complete evidence record, but it does not imply that the whole adoption proof,
the Text Model Lifecycle V1, or a second physical GPU node is usable. The clean
Windows VM is a second client device for the scoped P0-17/P0-31 evidence; it is
not a second physical GPU node. P0-11 has Actual PASS evidence for its declared
Phase 0 cancel/auth/usage/audit boundary; cancellation latency and later
enterprise Cost/Quota/RBAC/SSO extensions remain outside this component check.

本记录只勾选有完整真实证据的组件或跨进程单项。单项通过不代表 adoption proof 总门、
第二台物理 GPU 节点或完整 Text Model Lifecycle V1 已可用。干净 Windows VM 可作为 P0-17/
P0-31 的第二客户端设备证据，但不能替代第二台物理 GPU 节点。P0-11 的 Phase 0 取消、认证、
usage、失败审计和重启回读边界已有 Actual PASS；取消 latency 与后续企业级 Cost/Quota/RBAC/SSO
扩展仍不在本项范围内。

Credentials, principal configuration, private launch addresses and raw private
Codex archive content were deliberately not read into or copied to this report.
Safe proof JSON files are cited by filename only; their private values remain in
the Navigator proof directory and the authoritative event log.

凭据、principal 配置、私有启动地址和 Codex 原始私有会话内容没有被读入或复制到本报告。
这里只按文件名引用安全的 proof JSON；私有值仍保留在 Navigator proof 目录和权威 event log。

## Provenance, commands and environment / 来源、命令与环境

### Commit / PR

- Historical Navigator implementation source (V7): `cd0d6a564c653c6576e1bff68c05cb84d91e57ff`.
- Historical Navigator CI fix: `91a6349eca0b137e57b4f348b8508218826755d1`.
- Navigator final V8 package source: `a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe`, with
  97 exact committed inputs; package receipt `windows-v8-package.json` SHA-256
  `850e17fa78136c52a9a73d71f282ef7fb5e09e5e94709d97d92821456b0e44fc`.
- Navigator persistence boundary tests: `bc0dd31fb81d50b5b61398cd72f8f017f658a82b`.
- Navigator Product metadata boundary: `c53e9db6957bc260f89d060620ca9c3967f405dc`.
- Navigator Windows desktop/native proof source: `6b1d5e09a0622901f597a931863abdc3fecf653f` (historical v5 package proof recorded below).
- DeepSeek Harness source: tag `dsh-v0.1.3-alpha.1`, commit
  `d347e703908d0406b7a7ef80e3a0e594d86b2215`.
- Platform frozen CES/resolver revision: `30272145b9c11df9948465359479c3d95d08a1dc`.
- Platform native revision bundled in V8: `185527f82c83700d4f567ac563a393a2e1c18c87`.
- Exchange frozen runtime revision: `91c509904add26de941e2e14bc3b2b4752233d28`.
- Plugins frozen provider revision: `261a78fd36c7cb2da85a504a89ec7c9c353ecc19`.
- Windows GUI Codex resume proof: `.navigator/proof/windows-codex-ui-result.json`,
  SHA-256
  `49ff09a5d7bb8bf54c8bf0ce03c8c251a218b20132fe8483a28ccdf779f70de2`.
- Windows native matrix report source: `cb504d5350b64061bc4946192107e0c8`,
  `windows-native-matrix.json` (temporary agent evidence; the file is cited,
  not copied into this report).
- Workspace baseline used for the plan: `origin/main` at
  `eb5fb2cf1af5c3dd0cbadbe6167bc388b643e318`.
- The pinned source audit reports `UPSTREAM_CORE_PATCHES = 0`; no upstream Core
  patch was copied into this report.

### Commands

The source and component checks were run against the fixed source tree and
locked dependencies. The proof run used a private address file; its path and
contents are intentionally omitted.

```bash
node scripts/prepare-harness.mjs --root /tmp/cyrene-navigator-dsh-d347e703 --install --build --link
node /tmp/cyrene-navigator-dsh-d347e703/node_modules/typescript/bin/tsc \
  -p harness/tsconfig.json
CYRENE_DSH_ROOT=/tmp/cyrene-navigator-dsh-d347e703 \
CYRENE_MANIFEST_BINARY=/home/baijin/Dev/Cyrene/Cyrene-Platform/target/debug/cy-manifest \
  node --test harness/tests/*.test.mjs
uv run pytest -q
cargo +1.97.1 fmt --manifest-path native/Cargo.toml --all -- --check
cargo +1.97.1 clippy --manifest-path native/Cargo.toml --locked --workspace --all-targets -- -D warnings
cargo +1.97.1 test --manifest-path native/Cargo.toml --locked --workspace
node --test harness/tests/persistence.integration.test.mjs
```

The real Web proof was executed through the re-runnable proof observer with a
private `--address-file`, a real session and a real provider request. It used
the upstream remote-mux WebSocket session stream; an HTTP/SSE-shaped response
was not used as streaming evidence. The cancellation run intentionally stopped
its collector at 1800 ms, so its incomplete turn is an expected cancellation
boundary rather than a failed completion assertion.

真实 Web proof 使用可重跑 proof observer、私有 `--address-file`、真实 Session 和真实
Provider 请求，并通过上游 remote-mux WebSocket 订阅 Session stream；没有把 HTTP/SSE
格式当作实时流证据。取消测试的 collector 有意在 1800 ms 停止，因此未完成 turn 是
预期的取消边界，不是错误地宣称回答完成。

### Result and environment

- Local component checks: the latest Python persistence suite passed 17 tests;
  the earlier 13-test count is retained only as historical evidence. Strict
  TypeScript build passed with `skipLibCheck` disabled; the Navigator integration
  set passed with no failures or skips; Native Rust tests passed 18, with format
  and clippy clean.
- Navigator persistence boundary integration: four tests passed, zero failed and
  zero skipped at commit `bc0dd31fb81d50b5b61398cd72f8f017f658a82b`. The tests
  cover service/reader restart and takeover, an ambiguous lost append response
  followed by reconnect, an outage before append that prevents tool execution,
  and historical Tool result recovery without replay.
- Linux environment: WSL2 x86-64, NVIDIA GeForce RTX 5070 with 12 GB VRAM,
  driver `616.56`, PyTorch `2.11.0+cu130`, vLLM `0.25.1`, managed CPython
  `3.12.11`, and pnpm `11.7.0`. The real provider path used the pinned Qwen
  text model fixture and the vLLM compatibility settings recorded in the
  historical report; this remains an external provider environment, not a
  Reactor/Platform Phase 1 deployment PASS.
- Web topology: the recovery proof used two independent Web processes on the
  same Linux machine and a separate persistence service. This proves process
  fencing and recovery boundaries; it is not evidence for two physical devices
  or cross-device workspace path mapping.
- Windows native and installed package: the agent-provided
  `windows-native-matrix.json` report from source `cb504d5350b64061bc4946192107e0c8`
  records a real 13/13 matrix; Linux also reports 13/13 for the corresponding
  native checks, with one Windows-only Node pin row excluded from Linux Node-pin
  coverage. The final V8 package was built from Navigator source
  `a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe` with 97 exact committed inputs,
  no uncommitted files and no skip flags. Its NSIS SHA-256 is
  `792d21adbbc2b95945dd61bfe43c68097a3d93d1f987dcb6e1cbc07687943a90`; the
  installed desktop executable is `0acf4412a424c91139dcc23fd943491fb168f5a932f6a34e902bb6618f19bcd1`.
  Clean guest installation and V8 installed-native quality 14/14 provide the
  scoped P0-28 package evidence. P0-17/P0-21/P0-28/P0-31 are now Actual PASS;
  P0-12/P0-29 remain unchecked. P0-29 has first-launch and normal restart
  evidence, but no same-V8 upgrade-failure diagnostic, so its declared gate is
  not silently narrowed.

### Limitations and delivery state

- `LOCAL_ACCEPTANCE`: the safe proof records below are PASS in the declared
  Linux process topology and the final V8 Windows host/clean-guest topology.
- `REMOTE_CI_BLOCKED`: hosted Python, Rust and existing product checks passed;
  the fresh cross-repository integration run `34010881724` stopped at its
  missing `CYRENE_CROSS_REPO_TOKEN` guard before external checkout. This is an
  environment/credential blocker, not a source PASS.
- `REMOTE_CI_BLOCKED` (separate cause): the Navigator V7 general run for
  `dcfff664a8be0a38d828b684c8c90ae2e7ca73fe` (`34033941585`) had zero steps and
  a GitHub billing/failed-payments/spending-limit annotation. The related
  Product run `34033941576` and desktop run `34033941578` failed; these are not
  green source results. No hosted package or canonical acceptance is inferred
  from this set of runs.
- `REMOTE_CI_BLOCKED` (billing cause retained): later Navigator runs were not
  source-green evidence because GitHub billing/spending-limit annotations
  prevented the jobs from executing their declared steps. The installed V8
  package and the local clean-guest proof are therefore recorded separately
  from hosted CI status.
- `CANONICAL_PENDING`: Workspace PR4 is green at this evidence snapshot, but
  the Phase 0 adoption decision, P0-12/P0-29 and all later product gates still
  require their own canonical read-back. No gate is inferred from a green
  repository check.

## Compatibility ledger

兼容性台账记录当前固定 proof 实际验证的边界。

This ledger records the boundary actually exercised by the pinned proof. It is
not a promise about unverified upstream behavior.

| Boundary | Observed contract | Evidence and limitation |
| --- | --- | --- |
| DSH Session v2 | Header is versioned and retains the required identity, creation and seeded fields; event envelope uses continuous `seq`, Unix-millisecond `time`, `type` and opaque `data`. | The live proof persisted native events and restored them through the upstream Session APIs. Event vocabulary remains owned by DeepSeek Harness; Cyrene does not reinterpret it. |
| Session persistence | Cyrene is the single writable eventlog; append is ordered, single-writer and flushed only after transaction commit. Harness local JSONL persistence is disabled in the Navigator profile. | The adapter and real Python service tests cover append, flush, restart, batch retry and sequence fencing. This does not make the product Usage ledger complete. |
| SessionHandle and writer fencing | A writer has an epoch/token lease; takeover is explicit, and stale append/flush/renew operations are rejected. | The second Web process observed and took over epoch 11. It is a same-host process proof, not a second-device proof. |
| Restore and replay | Restoring a Session reads stored events; historical Tool Call and Tool Result records remain records and are not dispatched. A new continuation creates a new turn through the upstream AgentRegistry/Session controller. | Linux recovery, Windows Codex continuation and installed Windows explicit takeover passed. The read-only permission survived takeover; full permission projection and failed-input UX remain pending. |
| History loading | Session list/page and event reads restored the bounded 20-, 55- and 63-event records used by the proof. The installed Windows GUI also opened a Codex archive in read-only preview and continued it as a new Session. | No large-history benchmark or pagination/latency guarantee has been established; same-session writer takeover remains unproven. |
| Stream transport | The proof observer uses upstream remote-mux Session stream frames and records arrival ordering, terminal events and request/usage correlation. | This is evidence for the current pinned path only; it does not certify every provider or the full OpenAI API surface. |
| Compaction summaries | Navigator revision `c09e737` passes the controlled four-test compaction suite and a real CUDA summary stream. The historical v5 no-text result remains a compatibility boundary, but does not prove raw-stream loss. | No blanket compaction guarantee or new Phase 0 Actual PASS is claimed; the historical failure remains available for diagnosis. |

## Profile and Bundle

本节记录 Profile、Bundle 与插件策略的实际启动证据。

### Commit / PR

Navigator commit `c00d59c81feac9d682fca6a10aa92f304efaa56d` contains the fixed
`cyrene-navigator` Profile/Bundle integration and Cyrene adapters. The upstream
source remains at the exact DSH tag/commit above. The profile inventory is an
observed startup inventory, not a hand-written list. The redacted profile
authority record is `profile-authority-boundary-result.json`, SHA-256
`991ce972d4a65aa7a48676ba8529c5b8ebca813c5f3d27fa4f886020cf140db7`.

### Commands

The fixed source was prepared, installed with the frozen lockfile, built with
the official build command, and launched in the Linux Web profile. The profile
inventory was queried through the authenticated runtime diagnostic route; no
credential or launch address is emitted here.

### Result

The earlier Linux diagnostic `runtime-plugin-inventory.json` reports 153
inventory entries: 121 active and 32 inactive. The earlier authenticated Windows v5 startup inventory reported 155 entries,
with 6 Cyrene entries active and 4 entries disabled. The V7 authenticated
inventory receipt records the same host count and separately records the
`cyrene-navigator` default preset and its active
`@cyrene/navigator-harness/compaction` preset row. The explicit profile authority proof reports `PASS`
for the Cyrene extension boundary, with the persistence, native bridge,
session-control and Codex-import integrations active and no upstream Core
patch. The default agent preset reported by the live profile is
`cyrene-navigator`.

The profile explicitly disables the default JSONL session persistence, session
telemetry, default DeepSeek route and title-LLM defaults. This is an observed
configuration/diagnostic result, not merely a UI omission. Enterprise plugin
allowlists, signatures, administrator policy and upgrade governance remain
P4-13 work.

### Environment and limitations

Linux WSL2 and the installed Windows v5 profile both use the fixed DSH source
and frozen dependencies. The explicit profile authority proof and the fresh
Windows inventory support the scoped P0-08 Actual PASS; enterprise plugin
allowlisting, signatures and administrator upgrade governance remain P4 work.
Clean-machine readiness is not included in this PASS. The profile is proven as
a Bundle/runtime boundary; it does not make Navigator a unified control panel
or move Product authority into Harness Core.

## Exchange streaming and tool continuation

本节记录 Exchange 实时流与工具续答的实际证据。

### Commit / PR

Navigator commit `c00d59c81feac9d682fca6a10aa92f304efaa56d`, fixed DSH source and
the real Exchange-to-provider path used by the live proof.

### Result

`live-stream-native-v4.json` is `PASS` with the following bounded result:

- 119 assistant stream frames and 87 text deltas arrived in monotonic order;
  the observed stream span was 1841 ms and median inter-frame arrival was
  21.496 ms.
- The stream ended with a real completed `turn/end`; two provider request IDs
  and four usage records were correlated in the proof record.
- One structured Tool Call and one matching Tool Result were observed. The
  actual Rust `cy-manifest` utility returned an Artifact manifest, and the model
  consumed that result in its continuation. The proof record retains the
  artifact digest without copying private session content.
- The Cyrene eventlog contained 20 events for the completed proof, including the
  Tool Call and Tool Result. The proof was a real model/provider path, not an
  in-memory tool fake or an accepted-prompt assertion.

### Environment and limitations

The path ran in the WSL2 NVIDIA environment described above with the pinned text
model and vLLM provider. This proves the Phase 0 Exchange/Harness path; it does
not prove a Reactor-owned deployment, a Platform placement, or the Phase 1
model-consumption gate. Direct external Provider connectivity remains a valid
Navigator independent entry point, but it is outside this proof and does not
replace the required Exchange path.

<a id="exchange-product-route-and-ledger-slice"></a>

## Exchange Product route and ledger slice / Exchange Product 路由与台账切片

本节记录真实 Exchange Product route 已接入 adoption proof 的边界。它补充
Exchange streaming/tool evidence，并覆盖 P0-11 所声明的 Phase 0 request、usage、
认证、失败、取消和重启回读语义。

### Commit / proof

The Product route was exercised against Exchange revision
`99c0a8855650ef1d0eba5c7436eb9cecfdb9eeef`. The safe Navigator proof records are
`.navigator/proof/product-stream-native-v1.json` (streaming Tool continuation)
and `.navigator/proof/product-gateway-native-audit.json` (SHA-256
`b49072e525a32bd37a15ff7e55885fe53116cd169042e55444a4a7e269f032e2`), plus
`product-http-audit-correlation.json` (SHA-256
`82ec8cb6340bc3ff3272fd9c4189fe2052b1f796684aa32eee0078ada1834467`). Private
credentials, launch addresses and raw conversation content are not copied here.

### Result

- The real request used the persisted Exchange Product route, completed a
  streaming model response, invoked the Rust `cy-manifest` Tool and consumed
  the matching Tool Result in model continuation.
- Two Product SQLite ledger records were read back for the same proof path. The
  records retain the trusted actor/workspace references, credential reference,
  route identity, request ID and provider-observed usage. No raw token or
  prompt/message content is part of the ledger evidence.
- The authenticated audit API rejected an unauthenticated read with HTTP 401.
  A real provider failure returned HTTP 502 and its response `X-Request-Id`
  looked up the persistent audit row with terminal status `failed`; the row
  retained trusted actor/workspace, credential reference and `usageState:
  unknown`. This proves the failed-terminal correlation without copying raw
  request content.
- Plain streaming cancellation returned `cancelled` with `usageState: unknown`,
  not zero, and no retry created another model request. The response header ID
  matched the first stream event and the authenticated audit lookup. A Product
  gateway restart preserved four ledger records field-for-field and kept the
  Endpoint and Route identity stable; the resumed Session continuation returned
  `PRODUCT_GATEWAY_RESTART_OK`.
- Taken together with the cancellation record below, this is an Actual PASS for
  P0-11's Phase 0 Exchange cancel/auth/usage/audit boundary. Cancellation
  latency was not measured; full Cost/Quota/RBAC/SSO ownership remains P4 work.

### Environment and limitations

The proof ran in the WSL2 NVIDIA environment with the pinned Harness source and
the real vLLM provider path. It used the persisted Exchange Product route and
authenticated audit API; the product gateway was restarted and read back from
the same SQLite authority. Navigator can still connect to an external Provider
independently; this proof intentionally uses Exchange because that is the
adoption-proof boundary. Hosted CI status for the newer Exchange and Platform
revisions is tracked separately and is not inferred from this local proof.

## Cyrene persistence and recovery

本节记录 Cyrene 持久化与恢复的实际证据。

### Commit / PR and commands

Navigator commit `c00d59c81feac9d682fca6a10aa92f304efaa56d`; the persistence
adapter, real Uvicorn service and SQLite WAL store were tested using the
commands in the provenance section. The safe records are
`recovery-success-events.json`, `service-restart-final-result.json` and
`service-restart-continuation.json`.

### Result

- The second independent Web process explicitly took over epoch 11 and returned
  `RECOVERY_OK`. It observed 55 stored events, preserved the original 20-event
  prefix event-for-event, and retained one historical Tool Call/Result pair.
- The persistence service was SIGKILLed and restarted. The final recovery record
  reports 55 events with `eventsEqual: true`; the subsequent continuation passed
  as `SERVICE_RESTART_OK`, reached 63 events and added zero Tool Calls.
- A service SIGKILL boundary did not append an additional event; after restart
  the same writer resumed successfully. The installed Windows restart record
  separately retains a 32-event committed prefix through the runtime restart
  and reached 44 events with zero new Tool executions.
- The recovery path did not re-execute the historical Tool. New continuation
  events were appended only after the new writer/session controller path was
  established.
- The 20-event append proof and the restart records establish ordered event
  persistence, writer epoch fencing and a commit-visible flush boundary. The
  Python service test matrix also covers idempotent same-body batch retry and
  conflicting batch rejection as atomic operations.

### Environment and limitations

The two Linux Web clients in this component record were independent processes on
one WSL2 host, with a separate persistence process and SQLite WAL database. The
later V8 clean Windows guest is an independently installed second client device;
its read/takeover and stale-writer evidence is recorded in
[the final V8 Windows section](#windows-v8-final-installed-and-recovery). This
does not represent a second physical GPU node. Organization-level OIDC/RBAC
remains Phase 4. The scoped Product metadata boundary is evidenced below; its
complete sharing/permissions UX and the remaining enterprise Usage/Auth/Audit
extensions are separate unfinished acceptance items.

<a id="product-metadata-boundary"></a>

## Product metadata boundary / Product 元数据边界

本节对应 P0-18，记录 Navigator 产品元数据与唯一可写 Harness event log 的边界。

### Commit / commands

Navigator commit `c53e9db6957bc260f89d060620ca9c3967f405dc` adds the stable Product metadata ownership projection and four focused metadata tests. The root's scoped command was:

```bash
uv run pytest -q
```

The latest run reported 17 passed; the earlier 13-test report is historical.
The real service migration was also exercised against legacy rows whose owner was
unknown; the migration retained that unknown value instead of inventing an
identity. New Session creation records the trusted creator/owner
`adoption-owner` and Workspace association from the configured principal.

### Result and limitations

The metadata projection stores owner/workspace authority separately from the
single writable event log. It does not create a second writable message history,
and the existing event log remains the source for Harness Session events. This
meets the scoped P0-18 boundary check. Complete title, sharing policy, tags,
permissions and audit projection UX remains pending; those are later product and
enterprise work, not evidence that the metadata boundary is absent.

<a id="crash-and-replay-boundary"></a>

## Crash and replay boundary / 崩溃与重放边界

本节对应 P0-19，专门记录 persistence 在故障、重连和恢复时的真实边界。

### Commit / commands

Navigator persistence boundary tests were added at commit
`bc0dd31fb81d50b5b61398cd72f8f017f658a82b`. The focused command was:

```bash
node --test harness/tests/persistence.integration.test.mjs
```

### Result

Four real integration tests passed, with zero failures and zero skips:

1. service/reader restart and writer takeover preserve the committed event
   prefix and do not reorder the resumed append;
2. an ambiguous lost append response followed by reconnect leaves one
   committed batch and makes the identical retry idempotent;
3. a persistence outage before append prevents the owning Tool from executing;
4. restoring a historical Tool Result does not replay or dispatch that Tool.

Together these tests cover append-before/after failure boundaries, duplicate
batch recovery, disconnected readers observing the newer event prefix and the
non-executable replay rule. The tests use the real Python persistence service,
SQLite event authority and upstream Session APIs; they do not use a fake
SessionStore or a second message database.

### Environment and limitations

The run used the pinned DeepSeek Harness source on Linux WSL2 with the real
Navigator persistence service. This is a component Actual PASS for the P0-19
committed-eventlog and non-replay boundary. An outbound draft that failed before
commit was not preserved; that is an explicit current limitation, not evidence
that unsent drafts are durable. Runtime-crash Reload behavior remains under
investigation and is not counted as a PASS. The result does not prove two
physical devices, a physical network disconnect, Windows writer takeover, or
the complete Product metadata UX and Exchange audit experience.

## Rust stdio bridge

本节记录 Rust stdio 原生桥接的实际证据。

### Commit / PR and commands

Navigator commit `c00d59c81feac9d682fca6a10aa92f304efaa56d`; the existing Rust
binary is invoked by the thin Cordis adapter through inherited stdio. Native
format, clippy and test commands are listed above. The integration suite used
the compiled `cy-manifest` binary through the production request path.

### Result

The versioned protocol carries protocol version, request ID, method, params,
result, error, cancel and event messages. stdout is reserved for protocol
frames and stderr for diagnostics. The actual Tool Call in the live proof ran
through the Rust process, returned a manifest artifact result and correlated
with the matching Harness Tool Result. Native Rust tests reported 18 passed;
the TypeScript and integration checks reported no failures or skips.

### Environment and limitations

The inherited-stdio path was exercised on Linux. A later 11-test native bridge
set covers real subprocess cancellation, timeout, crash/EOF, protocol mismatch,
output bounds and reaping through the fixed upstream supervisor. The full Node
integration set now passes 17/17 with no skips at Navigator commit
`2abc7865cbe2aa66dff9986d0f6eacea6c7d1a8a`. The POSIX fault fixture remains a
Linux implementation detail; the separate Windows/native-child matrix below
closes the scoped P0-23 and P0-24 checks. Desktop restart/takeover remains
separate P0-21/P0-31 work; P0-20 and P0-22 are also supported by this report.

<a id="windows-native-matrix-and-desktop-sub-scenarios"></a>

## Windows native matrix and desktop sub-scenarios / Windows 原生矩阵与桌面子场景

本节只记录已提供的 Windows 原生与桌面子场景证据，不把局部通过扩展成完整
Windows package 或 Desktop gate。

### Evidence / 证据

The native agent report at source `cb504d5350b64061bc4946192107e0c8` records
`windows-native-matrix.json`. Its real Windows matrix reports 13/13, and the
corresponding Linux matrix reports 13/13. One row is a Windows-only Node pin
check; it is retained as a scope note and is not counted as Linux Node-pin
coverage. Navigator's Windows proof source is
`6b1d5e09a0622901f597a931863abdc3fecf653f`. The installed NSIS package was
exercised. Its immutable digest and later recovery-package digest are recorded
in [installed Windows recovery](#windows-desktop-recovery-and-faults).

### Result / 结果

- Windows first authentication, a real Rust Tool call, approval and cancellation
  UI sub-scenarios passed. The safe `proof-ui/fresh-v2*.json` records and
  screenshots are cited by filename only; no private launch material is copied.
- Before the desktop restart, the safe session evidence recorded 73 persisted
  events. After a normal exit/restart, history read succeeded. The earlier
  same-Session Continue then returned `SessionAlreadyOwnedError` because the
  desktop had no takeover UI. The later recovery package passed explicit
  takeover; the earlier failure is retained as history below.
- The matrix is sufficient for the declared native error/cancel and inherited
  stdio portability scope, so P0-23 and P0-24 are Actual PASS. It does not make
  the complete package/recovery gates pass. P0-30 now has separate installed-GUI
  evidence; clean-machine readiness, full recovery and package reproduction
  remain separate acceptance work.

### Environment and limitations / 环境与限制

The report covers real Linux and Windows native subprocess environments. The
Windows UI was run from an installed NSIS build; its exact digest is now
recorded, while clean-machine readiness remains pending. The sub-scenarios use
the same Cyrene persistence authority and do not prove a second physical device.

<a id="windows-codex-ui-resume"></a>

## Windows GUI Codex preview and new-session resume / Windows GUI Codex 预览与新会话继续

本节是 P0-27 的边界证据。它证明安装包中的导入预览和用户明确触发的新
AgentRun；它不把新 Session 继续误报成原 Session writer takeover，也不替代完整
Windows desktop gate。

### Evidence / 证据

The redacted result is retained at the local Navigator proof path
`.navigator/proof/windows-codex-ui-result.json`; its SHA-256 is
`49ff09a5d7bb8bf54c8bf0ce03c8c251a218b20132fe8483a28ccdf779f70de2`. The record
reports `result: PASS` for an installed GUI file chooser, read-only preview,
explicit Continue as a new session and GUI send. Raw private conversation
content is not copied here.

### Result / 结果

- New Session `codex-continue-b6c7ca93-1450-4e2a-a196-1224c41b56f1` persisted
  32 events; the new AgentRun boundary is seq 16, followed by freshly recorded
  permission/sandbox/policy events at seq 17/18/19.
- The imported history contained 2 historical Tool Calls and 2 historical Tool
  Results; they remained archive records and `newToolExecutions` was 0.
- The new turn reached the real Exchange route with request ID
  `chatcmpl-b3246e9df7624e9c9b1f66e5da9cf199`. Provider usage was
  `input=6103`, `output=8`, `total=6111`, and the trusted Product metadata
  returned workspace `adoption-proof` and owner `adoption-owner`.
- Additional bounded installed-package records are retained by filename and
  digest: `windows-desktop-subscenarios-result.json`
  (`e6924ded123bd2d69ab6ab268053d2dce73dc49b0975815ba30d04298195aa0d`) records
  8 successful requests with exact audit token correlation, Rust Tool,
  allow-once approval, visible Tool error, GUI stop, 73 events preserved across
  close/reopen and a visible notification after runtime Node kill.
  `windows-persistence-restart-result.json`
  (`06b6be2536974c6fd5a4ebc04d2d979343452009ee0db35c9d0fb8c37db48f31`) records
  service SIGKILL process transition `1351755` → `1462232`, continuation on
  Windows runtime `27968`, a 32-event prefix restored into 44 events, zero new
  Tool executions, and request `chatcmpl-fae443e1a28a472ba6a44f9b64dad329`
  with usage `6135/8/6143`.

### Scope and limitations / 范围与限制

This is Actual PASS for P0-27: the GUI explicitly created a new AgentRun, the
new turn re-evaluated permission/model/Workspace context, and historical Tools
were not replayed. The prior same-Session Continue failure
`SessionAlreadyOwnedError` was addressed by explicit takeover in the later
installed recovery package. Clean-machine readiness, full desktop recovery
and package reproduction remain unchecked. The records explicitly retain
failed outbound draft loss and the earlier misleading runtime-crash Reload
action; the latter now requests close/reopen in the recovery package. No
full recovery PASS is inferred from these bounded sub-scenarios. The restart
record is not evidence for a physical network disconnect or a second device.

## Codex import and safety

本节记录 Codex 导入与安全边界的实际证据。

### Commit / PR and commands

Navigator commit `c00d59c81feac9d682fca6a10aa92f304efaa56d`; the authenticated
Codex import route used the real Rust importer and the Cyrene eventlog. The
source archive itself is not included in this report. Safe records are
`real-codex-import-result.json`, `real-codex-continue-result.json` and
`codex-source-events.json`.

### Result

- The fixture came from real Codex CLI `0.152.1`. The conversion report records
  28 source records, 4 ordinary messages, 2 historical Tool Calls, 2 historical
  Tool Results, zero malformed records, one unknown record and the conversion
  warnings. Source identity, raw bytes/hash and the report are retained in the
  same authoritative eventlog; the report does not reproduce their private
  values.
- All 24 imported `cyrene/import-history` records in the source event proof have
  `executable: false`. Historical system/permission/tool content remains a
  readable record; it is not dispatched, replayed or placed in a new execution
  queue.
- The real backend Continue request returned HTTP 200, was non-duplicate, used
  the `cyrene-navigator` preset and supplied a new `cwd`. The model continuation
  proof reached 33 persisted events with zero new Tool Calls, confirming that
  history was not re-executed.

### Environment and limitations

The import ran through the authenticated Navigator Web route on the Linux
profile. A read-only archive can be listed and opened through the Web session
projection. The installed Windows GUI preview and explicit new-session
continuation are covered by [the bounded GUI proof](#windows-codex-ui-resume),
so P0-27 is an Actual PASS. The final V8 installed-package and clean-guest
records below separately close the scoped P0-17/P0-21/P0-28/P0-31 boundaries;
full permission projection and the P0-29 upgrade-failure diagnostic remain
unproven. Dynamic loopback allocation remains covered by P0-32, and P0-30 has
separate installed-GUI evidence.

## Cancellation sub-scenario

本节记录取消子场景的实际证据。

### Result

`live-cancel-v2-result.json` is `PASS` for the bounded cancellation proof:

- The collector intentionally stopped at 1800 ms; `completedTurnEnd: false` is
  the expected cancellation result.
- 77 assistant frames arrived before the stop, cancel was accepted, and the
  Cyrene eventlog persisted 15 events ending with `user-aborted`.
- The post-cancel vLLM observation reported zero running and zero waiting
  requests; the gateway recorded no traceback, uncaught exception or
  `ProviderInvocationCancelled` error.

`live-cancel-v2-recovery.json` is `PASS` and returned `CANCEL_RECOVERY_OK` on the
same Session. A separate real CES-to-Official-HTTP check demonstrated no retry
and no fabricated usage. The Product route cancellation record
`product-cancel-result-v1.json` (SHA-256
`1ed9532e9c7dc24c0e748e1b695eee66e1a45e0487dc5242c38d1eb0b8451f61`) returned
`cancelled` with usage `unknown`, not zero, and the recovered Session returned
`PRODUCT_CANCEL_RECOVERY_OK`. The Product gateway restart record
`product-gateway-restart-result.json` (SHA-256
`f8dd0368d377b05e80ecb72b8629153146a605d053635e2e8f42145f650a178a`) preserved
ledger rows and route identity. These records do not claim a cancellation
latency metric: the 1800 ms value is the intentional collector limit, not
measured cancel latency.

<a id="p0-11-closure"></a>

### P0-11 closure / P0-11 完整验收

The combined Product route and cancellation evidence now meets the declared
Phase 0 boundary for P0-11. Authenticated audit reads reject an unauthenticated
caller with 401; two successful real model requests have exact provider-usage
correlation; a real provider HTTP 502 is persisted as `failed` and can be read
back by its response `X-Request-Id`; and plain-stream cancellation is persisted
as `cancelled` with usage `unknown`, without a retry or fabricated zero usage.
The cancelled request's response/header ID and first stream event correlate to
the same audit row. A gateway restart preserves four rows and stable
Endpoint/Route identity, and the resumed Session continues successfully.

This is Actual PASS for P0-11 as scoped. The cancellation collector's 1800 ms
cutoff is not a latency measurement; the cancelled provider response did not
expose a final provider response ID, so the cancellation audit correlation uses
the unique in-flight request interval and the response/event request ID. Full
Usage/Cost/Quota/RBAC/SSO enterprise ownership remains later P4 work.

## Check mapping / 检查项映射

These are the 30 checked component IDs in the main plan. Every row has the
commit, command, result, environment and limitation fields above.

| Check ID | Status | Primary evidence |
| --- | --- | --- |
| P0-01 | ACTUAL_PASS | Fixed upstream provenance; historical lock and digest report |
| P0-02 | ACTUAL_PASS | Linux frozen upstream rebuild receipt `linux-v7-upstream-rebuild-20260906.json`; V7 source/build provenance |
| P0-03 | ACTUAL_PASS | Immutable pinned-source audit, `UPSTREAM_CORE_PATCHES = 0` |
| P0-04 | ACTUAL_PASS | [Compatibility ledger](#compatibility-ledger) |
| P0-05 | ACTUAL_PASS | [Profile and Bundle](#profile-and-bundle) |
| P0-06 | ACTUAL_PASS | V7 authenticated inventory: 155 host entries, 6 Cyrene host active, 4 defaults disabled; Browser delivery remains separate |
| P0-07 | ACTUAL_PASS | Observed disabled defaults and startup inventory |
| P0-08 | ACTUAL_PASS | [Profile and Bundle](#profile-and-bundle), explicit profile authority boundary proof |
| P0-09 | ACTUAL_PASS | [Exchange streaming](#exchange-streaming-and-tool-continuation) |
| P0-10 | ACTUAL_PASS | Matched Rust Tool Call/Result and model continuation |
| P0-11 | ACTUAL_PASS | [Exchange Product route and ledger](#exchange-product-route-and-ledger-slice) plus [cancellation closure](#p0-11-closure) |
| P0-13 | ACTUAL_PASS | [Cyrene persistence](#cyrene-persistence-and-recovery) |
| P0-14 | ACTUAL_PASS | Ordered append, retry, flush and event prefix |
| P0-15 | ACTUAL_PASS | Web takeover, restart recovery and continuation |
| P0-16 | ACTUAL_PASS | Persistence service SIGKILL/restart equality |
| P0-17 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery); isolated second client device, stale-writer fencing |
| P0-18 | ACTUAL_PASS | [Product metadata boundary](#product-metadata-boundary) |
| P0-19 | ACTUAL_PASS | [Crash and replay boundary](#crash-and-replay-boundary) |
| P0-20 | ACTUAL_PASS | [Rust stdio bridge](#rust-stdio-bridge) |
| P0-21 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery); native lifecycle, queued close and child cleanup |
| P0-22 | ACTUAL_PASS | Real `cy-manifest` Tool result |
| P0-23 | ACTUAL_PASS | [Windows native matrix](#windows-native-matrix-and-desktop-sub-scenarios) |
| P0-24 | ACTUAL_PASS | [Windows native matrix](#windows-native-matrix-and-desktop-sub-scenarios) |
| P0-30 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery) |
| P0-32 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery), dynamic loopback allocation |
| P0-25 | ACTUAL_PASS | [Codex import](#codex-import-and-safety) |
| P0-26 | ACTUAL_PASS | Non-executable historical records and no replay |
| P0-27 | ACTUAL_PASS | [Windows GUI Codex preview and new-session resume](#windows-codex-ui-resume) |
| P0-28 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery); exact package, static native runtime and installed 14/14 |
| P0-31 | ACTUAL_PASS | [V8 Windows installed/clean-guest recovery](#windows-v8-final-installed-and-recovery); service fault/restart, input recovery and desktop close/reopen |

P0-12, P0-29 and P0-GATE remain `[ ]`. All Phase 1–4, cross-cutting and final
V1 checks remain `[ ]`; this evidence cannot be used as a model lifecycle gate.

## English handoff / 英文交接摘要

The pinned DeepSeek Harness source and the `cyrene-navigator` profile now have
real Linux evidence for Exchange streaming, a Rust stdio Tool, Cyrene event
persistence, crash/replay boundaries, process/service recovery, safe Codex
import, the Product metadata boundary, the Product request ledger and the
Linux/Windows native matrix. Thirty of thirty-two scoped Phase 0 checks are
marked Actual PASS. The Linux exact Node/pnpm frozen upstream rebuild is the
scoped P0-02 proof. V8 provides an official 97-input Windows build from
Navigator source `a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe`, a clean-guest
installation, installed native 14/14, real Rust Tool continuation, second-client
writer fencing and desktop service/input recovery. The NSIS package digest is
`792d21adbbc2b95945dd61bfe43c68097a3d93d1f987dcb6e1cbc07687943a90`, and the
installed desktop digest is
`0acf4412a424c91139dcc23fd943491fb168f5a932f6a34e902bb6618f19bcd1`. P0-29
remains open because the same V8 package has no actual upgrade-failure
diagnostic; P0-12 and the adoption gate remain open. Hosted billing/spending-
limit blocks remain separate environment evidence. Navigator can still connect
to external Providers independently; the adoption proof itself still goes
through Exchange.

## Cross-repository delivery update / 跨仓库交付更新

- Platform previously verified revision: `47c7f0a37c4da73ce6c2a9608b0759ac0095a19f`.
  All seven hosted CI checks passed: [34014381461](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014381461),
  [34014381498](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014381498),
  [34014381531](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014381531),
  [34014381439](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014381439),
  [34014381550](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014381550),
  [34014379385](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014379385),
  [34014379365](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34014379365).
  This is repository CI evidence for the Platform contract and execution fabric;
  it is not a Reactor/vLLM deployment proof.
- Platform current native-wire revision: `185527f82c83700d4f567ac563a393a2e1c18c87`.
  It contains the empty `binary_chunk` presence fix and the additional wire-test
  CI integration. The latest hosted CI result for this revision was not read
  back in this evidence update; the Windows/Linux native matrix is recorded
  separately as local/agent-provided evidence.
- Official Plugins: `261a78fd36c7cb2da85a504a89ec7c9c353ecc19`,
  [Draft PR #9](https://github.com/DoHorizon-AI/Cyrene-Plugins-Official/pull/9).
  All branch and PR checks, including the actual Platform conformance gate,
  passed. Local full conformance: 47 passed, no skips; provider tests: 109
  passed; catalog evidence mapping and scoped Ruff passed.
- Exchange previously verified Product revision: `c84df4f78d9c2be3afe43bc3927b7dd3ab968d91`.
  All four hosted checks passed: [34014720236](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34014720236),
  [34014720144](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34014720144),
  [34014717559](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34014717559),
  [34014717542](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34014717542).
  These checks include the persisted Product route path and strict Product
  quality gates.
- Exchange latest branch revision: `91c509904add26de941e2e14bc3b2b4752233d28`.
  It adds the real CES regression for a role-only empty first stream chunk and
  pins Platform native-wire revision `185527f82c83700d4f567ac563a393a2e1c18c87`.
  The local core suite reported 42 passed tests; the previously recorded
  Product/integration slice reported 15 passed. All four hosted checks for this
  exact revision reported success: [34017104413](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34017104413),
  [34017104414](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34017104414),
  [34017103172](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34017103172),
  [34017103207](https://github.com/DoHorizon-AI/Cyrene-Exchange/actions/runs/34017103207).
  This revision is not yet a main/develop merge or canonical promotion; the
  real Product proof above is the evidence for P0-11.

- Navigator V7 source `dcfff664a8be0a38d828b684c8c90ae2e7ca73fe` has an official
  package record with 81 exact committed inputs and no skip flags. The general
  hosted run `34033941585` had zero steps because of the GitHub billing,
  failed-payments and spending-limit annotation; Product run `34033941576` and
  desktop run `34033941578` failed. These results are not a green CI or
  canonical package proof.

提交仍位于 `feat/text-model-lifecycle-v1`；上述远端检查不是 main/develop 合并证明。
Platform 的 fallback 超时回归测试先真实复现了终止错误被丢弃的问题，修复后 25 个服务
TCK 全部通过。JVM hosted boundary 已通过；本地缺 JVM wire fixture 的限制仍保留，
不把入口返回或跳过误报为真实 JVM worker 运行证据。

<a id="windows-desktop-recovery-and-faults"></a>

## Historical installed Windows interaction and recovery / 历史 Windows 安装包交互与恢复

This section preserves the earlier V5 interaction and recovery evidence. The
current package/recovery record is [Windows V7 official package and recovery](#windows-v7-package-and-recovery-update).

The interaction proof used installed NSIS package SHA-256
`5fbb3d004196fc8d60f984679c6c854caed465c21cadee4514e71f2f290cb8be`.
The subsequent recovery proof used rebuilt NSIS package SHA-256
`4bea2dc7818697e0d04de256de60f0e21b437f01037969486df9486fd460001b`
and MSI SHA-256
`d890931a9252f861ba030fe1c7e89e43e67b3d7b74667ff4c0939214090ea299`.
Bundled Native host SHA-256 was
`81b768e89908baa73e178b1ca50b569ac373e213b7d6c06be302946f24721fdf`;
Platform `cy-manifest.exe` SHA-256 was
`3c89a4544b9014fb2befddad01c7e69d1ff20029a64a2a9a4ed8802dc944b1b3`.
These identify tested artifacts; source delivery and hosted package reproduction
are tracked separately.

安装包由 `scripts/windows/build-installer.ps1` 构建，通过 NSIS 安装到当前用户应用目录。
GUI 在 Windows 交互会话中启动；CDP `Input.dispatchMouseEvent`、`Input.insertText`
和文件选择器驱动实际 WebView。恢复使用界面中的观察、选择和明确 Take over 操作。
关闭使用 `CloseMainWindow()`；两个客户端均正常退出，临时计划任务已移除。
凭据从私有配置注入，不进入本报告。GPU、服务与模型环境沿用本报告的固定 RTX 5070
/ vLLM / Exchange / Cyrene persistence 拓扑；两个 Windows Profile 位于同一物理主机。

| Scenario | Actual evidence | Boundary |
| --- | --- | --- |
| Chat, streaming, Rust Tool, approval, error, cancel | `windows-desktop-subscenarios-result.json`, SHA-256 `e6924ded123bd2d69ab6ab268053d2dce73dc49b0975815ba30d04298195aa0d`: 73 events, 4 Tool Calls; manifest result followed by model continuation; file absent before Allow once and present after; visible missing-file error; actual Stop produces user-aborted turn; 8 successful requests match authenticated Exchange usage | **P0-30 ACTUAL_PASS**; cancellation latency was not measured |
| Close/reopen and explicit takeover | `windows-desktop-recovery-v3-result.json`, SHA-256 `2372b6bfcd70e1c95329acac074d7cc0f1e664748fc7035e04614c4851e8446b`: observed epoch 1 → explicit takeover → epoch 2; original 73 events unchanged, then 83 events after WINDOWS_RESTART_OK; read-only permission retained; no historical Tool re-execution | The action works; complete P0-31 awaits the UI fixes below |
| Second Windows Profile | `windows-peer-takeover-v3-result.json`, SHA-256 `0165938cba607ba3c37c18b8f8c2f8c9f3585a0275b12adcbb8879714506f231`: UI request with expectedEpoch 2 returns 200; 83 original events preserved; old client input never becomes persisted history; peer reaches WINDOWS_PEER_TAKEOVER_OK with 93 events and no historical Tool execution | Same physical host; old-window ownership feedback and failed-input retention need fixing |
| Persistence process restart | `windows-persistence-restart-result.json`, SHA-256 `06b6be2536974c6fd5a4ebc04d2d979343452009ee0db35c9d0fb8c37db48f31`: service SIGKILL, visible connection refusal, restart against the same database/port, explicit new input reaches WINDOWS_PERSISTENCE_RESTART_OK; committed history unchanged | Failed input was cleared; service process fault is separate from network failure |
| Controlled TCP outage and recovery | `windows-network-recovery-v3-result.json`, SHA-256 `172f3ae237405c13b09ed14aac7ea9084f52f75686a42261403e800a428007b4`: persistence remains live while a TCP proxy closes active/new connections; during outage all 93 events and Exchange ledger remain unchanged; visible ECONNRESET; explicit new input after restoration reaches WINDOWS_NETWORK_RECOVERY_OK, 105 events total | Failed input was cleared; upstream later flushes four failed-turn inbox/start/end events, without turning the failed input into a user/message, model request or Tool execution |

Restart continuation request `chatcmpl-653be6b72a744e1887ad969bdfdf1568` had
provider usage 6245/5/6250. Peer continuation
`chatcmpl-20b3800409de4494a4d34c8999638996` had 6273/7/6280. Network recovery
`chatcmpl-5f282fe830774e258a6aa9f44b3834cc` had 6302/6/6308. Each was read
back through authenticated Exchange audit as `completed` with matching actor,
workspace, route and request identity. Values are input/output/total tokens.

The fenced window currently clears failed input without an ownership message.
Connection failure is visible but also clears input. The frontend fix must use
upstream submission state, preserve unconfirmed input and never auto-resend.
It must not infer durable delivery from DOM text or create another history.
A cold Profile also lacks local title projections; a read-only adapter will
use upstream `sessionQuery.readTitleSnapshots()` without activating Agents.
The runtime-crash notice now requests close/reopen; it no longer offers a
Reload action that only reloads a dead runtime URL.

<a id="windows-v5-package-and-recovery-update"></a>

## Historical Windows v5 package and recovery update / 历史 Windows v5 安装包与恢复增量

This bounded update records the newest installed-package evidence without
copying credentials, launch addresses, prompts or private session content.

本节只记录最新安装包和恢复证据的安全汇总，不复制凭据、启动地址、prompt 或私有会话正文。

- Navigator source is `cd0d6a564c653c6576e1bff68c05cb84d91e57ff`; the latest CI
  fix is `91a6349eca0b137e57b4f348b8508218826755d1`. The v5 build-source report
  `windows-v5-build-source.json` has SHA-256
  `53ad8a7da315291bf1284687e5be703f9b439e15079d18f2d3573d063152deb7` and
  records 58 managed files with all 58 source/build digests matching.
- The installed v5 NSIS package has SHA-256
  `387800d9c88bfb8f01837e187a8b8cd74fc09d7e4c4852f6b380d3d3f64b0637`; the
  MSI has SHA-256
  `0c67250ea48a660719b2f1535c6f156cbdb660d69849845dbc3e0d28a9394739`.
  A fresh authenticated profile inventory summary reports 155 entries, 6
  Cyrene entries active and 4 entries disabled. The bounded profile authority
  result is the basis for P0-08; this does not cover P4 enterprise allowlists.
  `windows-v5-profile-inventory.json` has SHA-256
  `fd44c83f0348b665b1ad8fb9bf87219d9f374233bee3e160f6026bb897cc0fe3`.
- Real installed runs assigned loopback ports `12599`, `12294` and `5851`.
  The old writer retained its input and exposed a clickable Restore action;
  network failure exposed an explicit retry, with no automatic resend. A
  Harness crash was visible, and a normal desktop reopen restored committed
  history. The bounded v5 native Tool count reached 5 in the recorded runs.
  These observations support the transport and profile component boundaries;
  they do not close the full desktop recovery or child-process lifecycle gates.
- The latest installed-v5 normal-close proof
  `proof-ui/windows-v5-normal-close-result.json` reports 9 owned processes and
  0 remaining processes at the recorded end time `10:13:21`. Its companion
  process snapshot is `windows-v5-final-process-tree.json`. This is evidence for a
  clean shutdown observation only; it does not prove child restart, takeover,
  or recovery after a changed browser origin, so P0-21 and P0-31 remain open.
  Their SHA-256 digests are respectively
  `00363fddf839da5f1c588bff47d3d0cfd6588638e4d2d49d3a7ab3a9f9987ef0`
  and `701331cdd6f13f56a7eed0c0dfc152ec464b648542f96943b5ff8778154714bb`.
- `windows-v5-service-recovery-partial-result.json` has SHA-256
  `96b54ae535eb2bf6eace640af59a4fe9c6c3a3b405cc8412b333c75ca0a7c275`.
  Its backend proof recovered the committed 158-event prefix, advanced epoch
  8 to 9 after runtime reopen, and completed with 170 events, 5 Tools and 34
  audit rows, with zero historic Tool replay. The companion
  `windows-v5-service-retry-manual-after-observed.json` has SHA-256
  `b2c69a351559422381936bfa448cfb5e2ddb5a2411ad94e0e8c146464c86fc58`.
  This proves the backend recovery boundary exercised in the record.
- The same desktop restart assigned a new OS port and therefore a new browser
  origin; the unsent composer draft was lost. The prompt was manually entered
  again only to finish the backend recovery proof. The backend's `409
  SESSION_ALREADY_LOCAL` was an ownership conflict, not a transport timeout,
  and was rendered as an unknown result that invited a repeated observe loop.
  An origin-independent retained/failed-input recovery fix is still under
  implementation and requires a fresh installed-package proof. P0-31 remains
  unchecked; P0-21 also remains unchecked because the complete child lifecycle
  and recovery gate are not proven.
- The historical v5 no-text summary result remains a compatibility boundary while
  ordinary replies succeed. Navigator revision `c09e737` passes the controlled
  four-test suite and a real CUDA summary stream; the historical failure was not
  proven to be raw-stream loss. No blanket compaction PASS is claimed.
  The latest hosted Navigator CI attempt for `91a6349...` did not start because
  of a GitHub billing/spending-limit annotation; this is reported as a remote
  environment block, separate from the earlier missing
  `CYRENE_CROSS_REPO_TOKEN` guard.

这组 v5 证据使 P0-08（Profile/Bundle 边界）和 P0-32（动态 loopback 传输）达到 scoped
Actual PASS；P0-02、P0-17、P0-21、P0-28、P0-29、P0-31 以及 P0-GATE 仍保持未勾选。

中文说明：P0-30 只按安装包交互证据勾选。同一会话重启接管的后端边界已在 v5 证据中
恢复，冷启动标题也已正确显示；旧窗口冲突提示与跨来源输入恢复仍需新安装包验证，完整恢复继续保持未
勾选。独立第二客户端设备、干净 Windows 环境和 hosted package 重建尚未验证。隔离的
Windows 虚拟机可用于第二客户端验收，不能替代 Phase 4 的第二台 GPU 节点。当前 CI 修复
已记录为独立 revision；最新 hosted jobs 因 GitHub billing/spending-limit annotation
未启动，不能从中推导测试结果。P0-GATE 与所有后续阶段总门仍未通过。

<a id="current-evidence-delta"></a>

## Current evidence delta / 当前证据增量

Recorded 2026-09-06. This bounded section supersedes only the current-summary
statements above; historical failures and their original evidence remain
unchanged. The current count is 30/32 checked Phase 0 component items. P0-12,
P0-29 and P0-GATE remain `[ ]`; P0-02 is an Actual PASS for the reproducible
upstream build path. The final V8 package/recovery record below is current; the
V7/V6 records that follow remain historical.

### 2026-09-10 Navigator live-head validation / Navigator 当前分支验证

Recorded 2026-09-10 against Navigator `origin/develop` at
`f83a701b885b3b6cfffde5af15c233b13904bdfb`, in an isolated task worktree. The
fixed DeepSeek Harness checkout remained tag `dsh-v0.1.3-alpha.1` at
`d347e703908d0406b7a7ef80e3a0e594d86b2215`, with `corePatches = 0`. This bounded
update records current Linux validation only; it does not rewrite the V8
package provenance or promote any Phase 0 gate.

在 Navigator `origin/develop` 的 live HEAD
`f83a701b885b3b6cfffde5af15c233b13904bdfb` 上，于隔离任务工作树重新执行验证。固定的
DeepSeek Harness 仍为 tag `dsh-v0.1.3-alpha.1`、commit
`d347e703908d0406b7a7ef80e3a0e594d86b2215`，`corePatches = 0`。本节只记录当前 Linux
验证，不改写 V8 安装包来源，也不提前提升任何 Phase 0 gate。

#### Commands and results / 命令与结果

```bash
node scripts/prepare-harness.mjs --root /tmp/cyrene-navigator-dsh-d347e703 --link
node /tmp/cyrene-navigator-dsh-d347e703/node_modules/typescript/bin/tsc \
  -p harness/tsconfig.json --pretty false
node /tmp/cyrene-navigator-dsh-d347e703/node_modules/typescript/bin/tsc \
  -p harness/tsconfig.client.json --pretty false
node scripts/windows/build-harness-client.mjs --root "$PWD"
CYRENE_DSH_ROOT=/tmp/cyrene-navigator-dsh-d347e703 \
CYRENE_MANIFEST_BINARY=/home/baijin/Dev/Cyrene/Cyrene-Platform/target/debug/cy-manifest \
  node --test harness/tests/*.test.mjs
uv run --frozen pytest -q
cargo +1.97.1 fmt --manifest-path native/Cargo.toml --all -- --check
cargo +1.97.1 clippy --manifest-path native/Cargo.toml --locked --workspace --all-targets -- -D warnings
cargo +1.97.1 test --manifest-path native/Cargo.toml --locked --workspace
```

- Host and client TypeScript compilation and the generated Windows harness
  client completed successfully. The full harness set passed `65/65`, including
  Codex import, non-executable historical Tool records, persistence restart,
  writer takeover and the real Web Profile restart proof. The Profile test's
  local provider tripwire is not external-Provider evidence for P0-12.
- The current Python persistence suite passed `22` tests. Native Rust tests
  passed `13` tests; native format and clippy passed with `-D warnings`.
- The Linux Tauri desktop crate command
  `cargo +1.97.1 test --manifest-path apps/desktop/src-tauri/Cargo.toml --locked`
  remains `NOT_RUN`: the host has no `pkg-config`, so `glib-sys`/`gio-sys` stop
  before crate compilation. Linux PowerShell and these local tests cannot
  substitute for interactive Windows package acceptance.

- 当前 host/client TypeScript 编译和 Windows harness client 生成均成功。完整 harness 为
  `65/65`，覆盖 Codex 导入、历史 Tool 不可执行、持久化重启、writer takeover 和真实 Web
  Profile 重启；Profile 使用的是本地 provider tripwire，不是 P0-12 的外部 Provider 证据。
- 当前 Python persistence suite 通过 `22` 项；native Rust 通过 `13` 项；native format 和
  `-D warnings` clippy 均通过。
- Linux Tauri desktop crate 命令因宿主缺少 `pkg-config`，在 `glib-sys`/`gio-sys` 编译前停止，
  仍记为 `NOT_RUN`。Linux PowerShell 和这些本地检查不能替代交互式 Windows 安装包验收。

#### Gate disposition / Gate 判定

- **P0-12 remains `[ ]` / `NOT_RUN`:** no authorized external Provider
  endpoint and credential were available in this run. The source/profile path
  remains capable of carrying an independent upstream Provider configuration,
  but no real external request was executed, so source, usage owner and
  credential boundary are not claimed as Actual PASS.
- **P0-29 remains `[ ]` / `NOT_RUN`:** no recoverable isolated Windows Guest or
  snapshot was available for a real installer/update controlled failure. No
  same-V8 upgrade-failure diagnostic was produced.
- **P0-GATE remains `[ ]`:** the fresh local Navigator validation strengthens the
  component evidence only. It does not replace the missing P0-12/P0-29 proof,
  hosted billing recovery, required canonical read-back or the declared
  Exchange adoption boundary.

- **P0-12 仍为 `[ ]` / `NOT_RUN`：**本次没有可授权使用的外部 Provider endpoint 和凭据。源码及
  profile 仍保留独立 Provider 配置路径，但没有执行真实外部请求，因此不把来源、usage owner
  和 credential boundary 宣称为 Actual PASS。
- **P0-29 仍为 `[ ]` / `NOT_RUN`：**没有可恢复的隔离 Windows Guest 或 snapshot 来执行真实
  installer/update 受控失败；没有产生同一 V8 包的升级失败诊断。
- **P0-GATE 仍为 `[ ]`：**本次 Navigator 本地验证只增强组件证据，不能替代 P0-12/P0-29、
  hosted billing 恢复、canonical read-back 或声明的 Exchange adoption boundary。

#### Delivery read-back / 交付回读

- Plugins branch `feat/unified-official-plugins` is at
  `ff34399ff6a1f00dcaa9c1417a839c7912779e5a`; its GitHub run
  [34509850248](https://github.com/DoHorizon-AI/Cyrene-Plugins-Official/actions/runs/34509850248)
  was rejected before steps because recent account payments failed or the
  spending limit needs to be increased.
- Platform branch `chore/legacy-surface-astrbot-20260910` is at
  `6025845caa5fbab599e810d5d81840d91819135e`; its GitHub run
  [34509946268](https://github.com/DoHorizon-AI/Cyrene-Platform/actions/runs/34509946268)
  stopped for the same account-level billing reason. Astrbot branch
  `feat/native-aot-enablement` is at `e8e16b2f`; no GitHub workflow run was
  observed for that push.
- 三个已授权分支均已完成远端 SHA 回读。上述 hosted CI 结果是环境阻塞，不是源码通过或失败
  证据；canonical main/develop 未被直接写入，原有 dirty roots 也已保留。

<a id="windows-v8-final-installed-and-recovery"></a>

### Windows V8 final installed package and recovery / Windows V8 最终安装包与恢复

This is the current V8 evidence boundary. It supersedes the earlier V7 package
and shutdown summaries for the checks explicitly listed below; those records are
kept later in this file as historical evidence. Credentials, launch addresses,
prompts and full event contents remain in the private Navigator proof directory.

这是当前 V8 证据边界。它只替代下方明确列出的 V7 安装与关闭摘要；V7/V6 原始失败和限制
仍在本文后部保留。凭据、启动地址、prompt 和完整事件内容继续留在 Navigator 私有 proof
目录，没有复制到本文。

#### Provenance and package / 来源与安装包

| Evidence | Commit / digest | Result and scope |
| --- | --- | --- |
| V8 source freeze | Navigator `a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe`; source receipt `windows-v8-build-source.json`, SHA-256 `37bc8143dae008e52bd5ef48f20ca30e5bc38dee2cd58c41e2cebd720cd0175c` | 97 exact committed inputs, no uncommitted changes, no skip flags, upstream Core patches = 0 |
| V8 package | `windows-v8-package.json`, SHA-256 `850e17fa78136c52a9a73d71f282ef7fb5e09e5e94709d97d92821456b0e44fc` | Official non-intermediate package; NSIS SHA-256 `792d21adbbc2b95945dd61bfe43c68097a3d93d1f987dcb6e1cbc07687943a90`, 52,603,218 bytes |
| Clean guest install | `windows-v8-cleanvm-install.json`, SHA-256 `0864fc50a517847ea1077cee3eafdc31435180eb85f12b5565a3acc629685217` | Installer exit 0; installed desktop SHA-256 `0acf4412a424c91139dcc23fd943491fb168f5a932f6a34e902bb6618f19bcd1`; data root/config preserved; no session mutation or model request |
| Installed native quality | `windows-v8-installed-native-quality.json`, SHA-256 `a04b0d29abf967ea33c7fc438cd87e040fdbd5196316dcd3c942eab73d23e46a` | Installed Windows Node/native paths: 14/14 passed, 0 failed, 0 skipped; static CRT binaries run without the old VCRUNTIME dependency |

V8 clean guest and host use the same installed desktop digest. The guest had no
development environment or system VC runtime installed. This closes the
declared P0-28 reproducible-package boundary. It does not claim P0-29, because
the same V8 package has not yet produced a real upgrade-failure diagnostic.

V8 clean guest 和 host 使用相同的 installed desktop digest；guest 没有开发环境，也没有
安装系统 VC runtime。上述结果关闭 P0-28 的可复现安装包边界。P0-29 仍不勾选，因为同一
V8 包尚未产生真实的升级失败诊断证据。

#### Real tool, second client and stale writer / 真实工具、第二客户端与旧 writer

- The clean guest completed the real Rust Tool → model continuation path. The
  persisted proof advanced from 53 to 67 events, from 3 to 4 Tool records and
  from 52 to 54 Exchange requests, with the expected artifact and continuation.
  The private backend record is `windows-v8-guest-native-tool-backend.json`,
  SHA-256 `0a683e83e7de82e52a771c7193b874c69213dcd9acc6a90a312ea09a499b0d0b`.
- The isolated clean Windows guest is the second client device for P0-17. After
  explicit takeover, the former host writer's append was rejected. The complete
  backend snapshot recheck `windows-v8-stale-writer-audit-check.json` has SHA-256
  `a512ec2922e1b08a3e5aa7c4335fa9b067c4aed8941253ec77c2187fa3cd685d` and
  confirms 67 events and 54 requests remained unchanged; the persisted field is
  `requestId`. This is a second client-device proof, not a second physical GPU
  node or Phase 4 multi-node acceptance.

- Clean guest 完成真实 Rust Tool→模型续答；持久化结果从 53→67 events、3→4 Tools、52→54
  Exchange requests，并得到期望 artifact。私有 backend record 为
  `windows-v8-guest-native-tool-backend.json`，SHA-256 为
  `0a683e83e7de82e52a771c7193b874c69213dcd9acc6a90a312ea09a499b0d0b`。
- 隔离的干净 Windows guest 作为 P0-17 的第二客户端设备。明确 takeover 后，原 host writer
  的 append 被拒绝；完整 backend snapshot 复核 `windows-v8-stale-writer-audit-check.json`
  （SHA-256 `a512ec2922e1b08a3e5aa7c4335fa9b067c4aed8941253ec77c2187fa3cd685d`）确认 67
  events、54 requests 完全不变，实际持久化字段为 `requestId`。这不等于第二台物理 GPU
  节点，也不关闭 Phase 4 多节点验收。

#### Service restart, retained input and normal close / 服务重启、输入恢复与正常关闭

The guest persistence service was stopped and restarted during the V8 proof.
The explicit takeover advanced epoch 4→5 and the controlled failure left the
database at 68 events, 4 Tools and 54 requests. After the next startup, the
client explicitly took over epoch 5→6, restored the retained input without
retyping, and left history unchanged until the user explicitly sent it. A
durable receipt then advanced the record to 77 events and 55 requests, kept 4
Tools, and removed the local retained copy. The safe summary is
`guest-v8-input-recovery-summary.json`, SHA-256
`cd488907157aa5ba2f811d462455cc5d1ff4701a3cdceb6a6eacb9441b7a8542`.

The V8 queued-close proof targeted only the visible main Tauri window. The first
clean-guest close receipt `windows-v8-cleanvm-normal-close.json` has SHA-256
`2312828d562c894aeba335acda17a50a6ebc37b31110f328c79ea358dd488b6f`, waited
925 ms, observed 9 owned processes and reduced both original CDP/Harness
listeners to zero. A second main-window close receipt
`windows-v8-cleanvm-close-after-outage.json` has SHA-256
`437f63f65c47aa1a30febeab630672be83425d72d4fd9c472e13bc6d28922ebd` and
completed in 868 ms. Both records used the visible main window only; the old V7
internal-message-window traversal and forced cleanup remain historical failure evidence.

V8 proof 中 persistence service 被停止并重启；明确 takeover 使 epoch 4→5，受控故障后数据库保持
68 events、4 Tools、54 requests。下一次启动明确 takeover 5→6，Restore input 恢复原文本且不
重打，history 在用户明确 Send 前保持不变；收到 durable receipt 后变为 77 events、4 Tools、55
requests，local retained copy 为 0。安全摘要为 `guest-v8-input-recovery-summary.json`，SHA-256
为 `cd488907157aa5ba2f811d462455cc5d1ff4701a3cdceb6a6eacb9441b7a8542`。

V8 queued-close 只针对可见主 Tauri window。第一次 clean guest 关闭 receipt
`windows-v8-cleanvm-normal-close.json` SHA-256 为
`2312828d562c894aeba335acda17a50a6ebc37b31110f328c79ea358dd488b6f`，耗时 925 ms，9 个
owned processes 与原 CDP/Harness listeners 均归零。第二次主窗口关闭 receipt
`windows-v8-cleanvm-close-after-outage.json` SHA-256 为
`437f63f65c47aa1a30febeab630672be83425d72d4fd9c472e13bc6d28922ebd`，耗时 868 ms。两次
均只关闭可见主窗口；旧 V7 遍历内部消息窗口和强制清理记录继续作为历史失败证据。

#### Current decision / 当前判定

These records are Actual PASS for P0-17, P0-21, P0-28 and P0-31 within their
declared scopes. P0-29 remains `[ ]` for the missing same-V8 upgrade-failure
diagnostic; P0-12 remains the independent external-Provider follow-up, while
the adoption proof itself continues to require Exchange. P0-GATE and all
Phase 1–4, cross-cutting and final lifecycle checks remain pending.

上述记录在各自声明范围内使 P0-17、P0-21、P0-28、P0-31 达到 Actual PASS。P0-29 因缺少同一
V8 包的升级失败诊断仍为 `[ ]`；P0-12 仍是独立外部 Provider 入口的后续工作，而 adoption
proof 继续必须经过 Exchange。P0-GATE 以及 Phase 1–4、横向和最终生命周期检查仍未通过。

<a id="v8-frozen-backends-and-handoff"></a>

### V8 frozen backends and handoff / V8 后端冻结与交付

At 15:48 UTC the owned Exchange service was stopped normally and restarted
against the same Product database from exact clean source commits. The old
Exchange `99c0a8855650ef1d0eba5c7436eb9cecfdb9eeef` / CES
`185527f82c83700d4f567ac563a393a2e1c18c87` launch record is historical; it is
not relabeled as execution from newer branch heads.

15:48 UTC，受控 Exchange 服务正常停止，并从精确且干净的源码提交使用同一 Product 数据库
重新启动。旧 Exchange/CES 启动记录保留为历史，不把它改写为来自新分支 HEAD 的运行证据。
Windows V8 安装包、Navigator persistence 和外部 vLLM 均未因本次后端冻结而改变。

| Runtime role | Exact source / artifact |
| --- | --- |
| Navigator desktop and persistence | `a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe` |
| Exchange Product gateway | `91c509904add26de941e2e14bc3b2b4752233d28` |
| Platform CES and resolver | `30272145b9c11df9948465359479c3d95d08a1dc` |
| Official model provider connector | `261a78fd36c7cb2da85a504a89ec7c9c353ecc19` |
| V8 bundled cy-manifest source | `185527f82c83700d4f567ac563a393a2e1c18c87` — independent of the Linux CES source |
| Running CES binary SHA-256 | `923480ad3a88d3f37038b1d7ea462f43377b74a056199956040325fc1f1ddfa7` |
| Resolver binary SHA-256 | `bff45fd4f589fcca62502ed1123817c9e47fa38d80e57c04e773b5ada4764ac0` |

`cargo build --locked` succeeded with Rust 1.96.1 for CES and resolver.
Navigator and Exchange Product `uv sync --locked --dry-run` both reported no
changes. Module origins, installed distribution versions, lock hashes and all
tracked source file hashes were recorded before the fresh start.

CES/resolver 使用 Rust 1.96.1 和 locked Cargo 构建成功；Navigator 与 Exchange Product
两个 Python 环境的 locked sync dry-run 均为 no changes。启动前已记录模块来源、已安装依赖
版本、锁文件哈希与全部 tracked source file 哈希。

The same installed V8 guest then completed a new real GUI Rust Tool and model
continuation: **77→90 events, 4→5 Tools, 55→57 Exchange requests**. Both new
requests are `completed` with final provider Usage; all 77 prior events are
unchanged. The initial read-only audit checker incorrectly expected the status
literal `succeeded`; the persisted contract uses `completed`. The immutable
snapshot was checked again against that actual contract without sending another
model request.

同一 V8 Guest 随后完成一次新的真实 GUI Rust Tool 与模型续答：**77→90 events、4→5 Tools、
55→57 Exchange requests**。两条新请求均为 `completed`，带最终 provider Usage，原 77 个
事件不变。首次只读校验误用了 `succeeded` 状态常量；根据实际 `completed` 契约复核同一
不可变快照后通过，没有为修正校验器重发模型请求。

| Receipt | SHA-256 | Scope |
| --- | --- | --- |
| `windows-v8-frozen-backends-start.json` | `c5273ec7e1844ff30026e7b5a2d9e0892e247ae9a619a02af82df8c9d38d5943` | Exact source start, same Product database, running CES digest |
| `v8-frozen-backend-dependencies.json` | `4c7f8daafc63a4fe65f12845049a48ec30cd9ce58660577db768388fe0264056` | Locks, Python module/distribution closure and Rust binaries |
| `guest-v8-frozen-backend-tool-summary.json` | `6d0640cfe20a6dde42f7882ef236f34a6e91d059294dd267602778efd2417f3c` | 153 WebSocket frames, one Tool, continuation, retained input 0 |
| `windows-v8-frozen-backends-tool-audit.json` | `93514ad548ca5dfa041cbdba2d8244f0875ad0fd9152fa7d9b58979892455bfc` | Complete prior prefix and two final provider usage records |
| `windows-v8-frozen-backends-audit-validation.json` | `b42fe1f5834c2d3b9e536ed9b6f1c899bbbba732936c0260e4e2f353bdaf26bc` | Actual `completed` status contract verified, no repeated request |
| `windows-v8-handoff-connector-validation.json` | `f08d854da8e12d07f3841987515417ae723d42cea8d56417953a5170b7adda34` | Windows PowerShell 5.1; 9 passed, 0 failed, 0 skipped, ValidateOnly |

The complete staged runtime and installed V8 guest runtime have identical
content maps: 24,382 regular files, no missing, extra or changed paths. Both
maps have SHA-256 `1b2fd08a483fc050e6cd0e6cdfd477220d39f06f10f0b085899ec9505fe096a3`.
The independent comparison receipt `windows-v8-complete-runtime-parity.json`
has SHA-256 `6b919ec0874b7a1c74dd8f6d0377cb709a83dca3105975227e9f9339db740b9c`.
This is a complete content comparison, separate from the PowerShell wrapper's
failed exit-code observation; it does not assert that wrapper passed. All 97
committed Windows source inputs were rechecked before collecting the maps.

staging 与 V8 Guest 安装后的完整 runtime 内容清单一致：24,382 个常规文件，无缺失、额外
或变更路径，哈希如上。本结论来自完整 JSON 清单的独立解析与对比，不把 PowerShell
wrapper 未成功捕获的退出状态写成通过；采集前另行复核了 97 个 committed source inputs。

The colleague handoff instructions are in [Navigator V8](../handoff/cyrene-navigator-v8/README.md).
The final archive adds the complete five-repository `release-lock.json`, source
and dependency locks, exact NSIS installer, checksums and sanitized acceptance
records. Its Workspace SHA is generated after the documentation commit, avoiding
a self-referential source SHA. Every repository is explicitly classified as a
pushed task branch or a merged base; no local acceptance is labeled canonical.

同事交接入口见上述 Navigator V8 说明。最终包补入五仓库的完整 release lock、源码与依赖
锁、精确 NSIS 安装包、校验和及脱敏验收记录。Workspace SHA 在文档提交后生成，避免自引用。
分支是否推送与是否合并分别记录；本地验收不等同于主线已合并。

<a id="windows-v7-package-and-recovery-update"></a>

### Historical Windows V7 official package and recovery update / 历史 Windows V7 正式包与恢复增量

V7 was built from Navigator source `dcfff664a8be0a38d828b684c8c90ae2e7ca73fe`.
The source record has 81 exact committed inputs, `containsUncommittedChanges:
false`, `skipFlags: []`, and upstream Core commit
`d347e703908d0406b7a7ef80e3a0e594d86b2215`. The official package record has
`buildComplete: true`, `intermediateSnapshot: false`, `exactCommittedInputs:
81`, and no skip flags. The installed-host installer exited 0, but the host
record explicitly has `cleanVm: false`; this is an installed same-host proof,
not clean Windows/VM acceptance. The package record's source-proof digest is
`fcabcfc13674cc3026a0e21a224bb14beb84eb10bc81b05953946cd7671a1f9d` and its
build-log digest is
`d49e5aa62628a2c946aed5fcc6c19234c33247041f9f9538a16bf40c6ab52317`; the
installed executable digest is
`5ea48d255dd6237e332a9941649706dcde25087191f9c1633757e32c6e9f29cd`.

| V7 record | SHA-256 | Observed boundary |
| --- | --- | --- |
| `windows-v7-build-source.json` | `fcabcfc13674cc3026a0e21a224bb14beb84eb10bc81b05953946cd7671a1f9d` | 81 committed inputs, no uncommitted changes, no skip flags |
| `windows-v7-package.json` | `bccac1addc2cfccca2a906fa2785075740a32f217bbb1b55eeb0aac59235aaa7` | Official build complete; NSIS `a866353d03fb8d9def65538588bff4bd080efb1fa11e9c3e57a343778590c25f`; MSI `adcbafe683bf9cd618e1d41c362d76bc7d3be428f4b5c64ad684f3cbe558f42b` |
| `windows-v7-host-install.json` | `8f29a6d434a9401bacfd008ce879fc50de1da18485b709b774d8175a64c969c6` | Real host installation, exit 0, `intermediatePackage: false`, `cleanVm: false` |
| `windows-v7-client-quality.json` | `e751bb8e4bae9785045a4817e273800794c2549f4d8ef7986f2bc682279d3697` | Strict TypeScript PASS, loader build PASS, 23/23 tests, 0 failures, 0 skips; installed acceptance field remains pending |

The existing Tauri quality result remains 17/17; V7 does not turn that
component result into a complete installed-desktop gate. The host recovery
records start from 171 events, 5 Tools and 44 audit rows. The original input was
restored without manual retyping while a new composer draft stayed protected.
The local store-lock fault left the event history unchanged and exposed an
explicit retry; an explicit local save retry succeeded while offline without
resubmitting the model. A normal close/reopen used a new loopback port and
restored retained native request `b910ba9a-ff3a-438b-a57e-46e613104b39`, with
the host trace advancing writer epoch 10→11 on takeover and 11→12 on reopen.
These are bounded recovery subscenarios,
not a clean-VM or second-device proof.

The online explicit Send then advanced the persisted record from 171 to 182
events, from 44 to 46 audit rows, and retained 5 Tools. The historical prefix
was preserved, no historical Tool was replayed, the completed turn was turn 16,
the exact assistant marker `WINDOWS_INPUT_RECOVERY_V6_OK` was observed,
compaction completed, and
the post-ack local-copy/UI counts were both zero. The redacted records are
cited by digest only:

| V7 recovery record | SHA-256 |
| --- | --- |
| `windows-v7-recovery-summary.json` | `2d69d70ba00804b57a3a46ea5e22cf9213aeb875d9bf78bc4c77b1b42ec36f3a` |
| `windows-v7-reopen-restored.json` | `b2fa60d85d39bd41f391844899f56404ebb2a2cfa524988c9118e35867c49c26` |
| `windows-v7-original-input-restored.json` | `7bca4ca8ae95887f2b8b8c4b061a0e7af7659066b262f0b20f3dbd4a271b825f` |
| `windows-v7-local-fault-result.json` | `a6b9a27523f2b13ec964440919f0c833f9a030d6edd3bc967562c3a5f1a18b8e` |
| `windows-v7-local-retry-result.json` | `6fcf3fc0710b2048a3b05d1e7492a4709b0eb9be28069dd9cedbe445456a159a` |
| `windows-v7-online-resend-summary.json` | `36e269ff31aff639734ab7e583d904e0d20cc456cb82cd530143e155895f7039` |
| `windows-v7-delivery-ack-observed.json` | `601c20fca4634445f74c6e856b89b5ec317516c332b977e8214155bc97a6f76e` |
| `windows-v7-delivery-completed-summary.json` | `1fbfdf48846b4c51d147bb83fe5c0295f00f1e3d15127622e0f1e80e417f7822` |

The private before snapshots `windows-v7-recovery-before.json`,
`windows-v7-local-fault-before.json`, `windows-v7-online-resend-before.json`
and `windows-v7-after-reopen-before-send.json` share SHA-256
`d5714be78b60d65b15bad3adf41b1d7ebbc4463d2b88bb06eeda4e7bd78810e5`; the
online after snapshot `windows-v7-online-resend-after.json` has SHA-256
`ee453feff738e4cd2773cb32d190ce486780c774fa876eed1d8073fb06f146a5`. Their
event contents are intentionally not copied here.

The current V7 records support the already-scoped desktop interaction and
recovery subscenarios. The Linux upstream rebuild is an Actual PASS for P0-02,
while the V7 staged/installed resource parity remains a bounded filesystem
audit only. P0-28 remains unchecked: clean Windows validation found that both
`native/cyrene-native-host.exe` and `native/cy-manifest.exe` import
`VCRUNTIME140.dll`, so the package has not proved a self-contained CRT runtime.
Do not install that dependency as a substitute for package acceptance. P0-29
remains open because `cleanVm` is false; P0-31 and the P0-GATE remain open
pending the final guest/multi-device and whole-proof acceptance.

<a id="windows-v7-installed-resource-audit"></a>

### Historical Windows V7 installed-resource audit / 历史 Windows V7 安装资源审计

The read-only staged-to-installed comparison covers 49 selected runtime files: 26
files outside the managed Bundle and 23 files in
`profile-template/node_modules/@cyrene/navigator-harness`. Both trees have the
same set, zero missing or extra files, zero differing file digests and zero
symlinks in the managed Bundle. The private receipt contains the per-file
staged/installed sizes and SHA-256 values; it is mode `0600` and contains no
credentials, prompts or raw session content.

只读 staged→installed 对比覆盖 49 个选定 runtime 文件：managed Bundle 外 26 个文件，
以及 `profile-template/node_modules/@cyrene/navigator-harness` 中的 23 个文件。两边
文件集合相同，没有缺失或多余文件，逐文件 digest 差异为 0，managed Bundle 没有
symlink。私有 receipt 保存每个文件的 staged/installed 大小和 SHA-256，权限为 `0600`，
不包含凭据、prompt 或私有会话正文。

| Resource audit | Value |
| --- | --- |
| Private receipt | `Navigator/.navigator/proof/windows-v7-installed-resource-audit.json`; SHA-256 `cb031e2abda7df217183cd6108e96b81c778e4432b8f0c14c8c61d215d649554`; mode `0600` |
| Source / package / install refs | Navigator `dcfff664a8be0a38d828b684c8c90ae2e7ca73fe`; `windows-v7-build-source.json` SHA `fcabcfc13674cc3026a0e21a224bb14beb84eb10bc81b05953946cd7671a1f9d`; `windows-v7-package.json` SHA `bccac1addc2cfccca2a906fa2785075740a32f217bbb1b55eeb0aac59235aaa7`; `windows-v7-host-install.json` SHA `8f29a6d434a9401bacfd008ce879fc50de1da18485b709b774d8175a64c969c6` |
| Runtime manifest | staged/installed byte-equal; SHA `3acf138e802cbc62a6c12ff833342e6d1d675f6ffe85959a3413502c5915895b`; Node `24.13.0`, pnpm `11.7.0`, DSH `d347e703908d0406b7a7ef80e3a0e594d86b2215` |
| Managed Bundle digest | staged = installed = manifest `sha256:b3954a815c803d9a386f1aff37a645be6bf9534355f7cda8d40ed6057c6e41fb`; `dist/client.js` SHA `a400a4a9c595b92d1542c2169a49c970240a18f1773491c13ee2ea1068b0973e`; `cordis.patch.yml` SHA `783f839ebc78d59d893c5e0a26f58aa19af4ac3cce4dea745da2d11308c54cc3` |
| Native/runtime files | `node.exe` SHA `d14ba95cdce1ef7dc9ad3ac74949ca5db38b27378ee30f30a23cf26f9e875a11`; `cyrene-native-host.exe` SHA `81b768e89908baa73e178b1ca50b569ac373e213b7d6c06be302946f24721fdf`; `cy-manifest.exe` SHA `3c89a4544b9014fb2befddad01c7e69d1ff20029a64a2a9a4ed8802dc944b1b3` |
| V7 package artifacts | NSIS SHA `a866353d03fb8d9def65538588bff4bd080efb1fa11e9c3e57a343778590c25f`; MSI SHA `adcbafe683bf9cd618e1d41c362d76bc7d3be428f4b5c64ad684f3cbe558f42b` |

The audit result is `PASS_SCOPED_INSTALLED_RESOURCE_PARITY`, not package
acceptance. The installed host exited 0 on the existing machine, but the clean
Windows check found `VCRUNTIME140.dll` imports in both native binaries. P0-28
therefore remains `[ ]` until the package supplies or otherwise proves its
self-contained CRT runtime; installing the CRT on the guest would hide the
packaging defect and is not acceptance evidence. The V7 host record also has
`cleanVm: false`. The absence of a public Cyrene native-host license text is
recorded as a private-proof redistribution limitation, not as an invented
license or an unauthorized blocker.

该审计结果是 `PASS_SCOPED_INSTALLED_RESOURCE_PARITY`，不是安装包验收。现有机器上的
installer exit 0 不能替代 clean Windows 证据；clean Windows 检查发现两个 native binary
都导入 `VCRUNTIME140.dll`。因此 P0-28 仍为 `[ ]`，要等包提供或证明自包含 CRT runtime；
在 guest 上自动安装 CRT 会掩盖打包缺陷，不能作为验收证据。V7 host record 同时明确
`cleanVm: false`。Cyrene native host 缺少公开 license 文本只作为私有 proof 的公开再分发
限制记录，不推断不存在的 license，也不把它当作未授权 blocker。

### Historical Windows V8 static-runtime candidate / 历史 Windows V8 静态运行时候选

The V8 static-runtime candidate was frozen from Navigator source
`a297d1cae54c5fbb8bffa68d748c59a9e7c1aabe`. The source receipt
`windows-v8-build-source.json` has SHA-256
`37bc8143dae008e52bd5ef48f20ca30e5bc38dee2cd58c41e2cebd720cd0175c`, records 97
exact committed inputs, no uncommitted changes, no skip flags and
`upstreamCorePatches: 0`. Its Windows MSVC native build uses static CRT linkage;
the candidate `cyrene-native-host.exe` and `cy-manifest.exe` digests are recorded
in the receipt.

The candidate quality receipt `windows-v8-static-native-quality.json` has SHA-256
`6cd0183b7f7683a4331479821ee51a61c8532db5f23c3b35120cb581e0cf432c`. It reports
14/14 Windows native integration checks plus 5/5 PE import regression checks,
19/19 total, with zero failures and zero skips. This is candidate quality
evidence, not installed-package acceptance.

The isolated clean-guest candidate preflight receipt
`windows-v8-cleanvm-static-native-preflight.json` has SHA-256
`9c1f4faf274664662f560f5a3dd8ec2e1209697eece08ce8de54bb71904e215e`. With no
system VC runtime installed, inherited stdio and `cy-manifest` completed and
returned the expected Artifact (`sha256:86198b4ac9e46d0f4d07f227a278e44aa8fec96796a7a9814a2a5f4db7e6d4b1`);
the installed V7 files were unchanged. The receipt scope is explicitly an
uncommitted static-build candidate preflight, with `installedPackageAcceptance:
false`, `sessionMutation: false` and `modelRequest: false`. It therefore does
not prove a final V8 installed package or first-launch gate. The V7
`VCRUNTIME140.dll` clean-VM failure remains historical evidence; P0-28, P0-29,
P0-21, P0-31 and P0-GATE remain unchecked, and the count stays 26/32.

新一轮 Navigator CI runs `34037983786`、`34037983796`、`34037983787` 没有提供 source-green
证据；general jobs 为 0 steps，annotation `101499455770` 仍指向相同的
billing/failed-payments/spending-limit 环境原因。因此不能从这些 runs 推导 V8 安装包或
P0 gate 通过。V7 的 `VCRUNTIME140.dll` 失败记录和限制继续保留，不用候选 preflight 覆盖它。

### Historical Windows V8 installed host and shutdown diagnostics / 历史 Windows V8 安装主机与关闭诊断

The V8 host installation completed with installer exit 0 and
`intermediatePackage: false`. Receipt `windows-v8-host-install.json` has
SHA-256
`7c1f5a8676514582a7428a01a9d7007fa561098c9a9a5992c94072c5025959c1`; its
record still says `cleanVm: false`. The installed-native quality receipt
`windows-v8-installed-native-quality.json` has SHA-256
`a04b0d29abf967ea33c7fc438cd87e040fdbd5196316dcd3c942eab73d23e46a` and reports
14/14 native integration checks on the installed Windows Node and native paths,
with zero failures and zero skips. This is a separate installed-package result;
it must not be added to the earlier candidate quality result of native 14/14 plus
PE 5/5 = 19/19.

The V7 guest shutdown failure produced a minidump with SHA-256
`d67a109cbd3edea5afd6ba2159dddfb50625135e0e1eb56d8a021c2e7c2dcd7a` (PID 9064,
created `2026-09-06T12:58:12`). Guest DLL expansion reached
`GetMessageW → tao::EventLoop::run`; V7 and V8 `.text`/`.pdata` are byte-identical,
so the mapping is only a reference to the V8 PDB and does not claim a matching V7
symbol. A read-only 17-byte runner check confirmed `ExitWithCode(0)` and
`HandlingMainEvents`.

The previous automation sent `WM_CLOSE` while traversing all owned visible and
hidden windows. That automation can interfere with the close path, so it cannot
establish a product root cause or a normal-shutdown PASS. `WM_NULL` succeeded for
the same session but did not make the process exit. PID 9064 was force-cleaned
before preparing the V8 guest installation; that cleanup is not normal-close
acceptance. The final comparison is deferred to the new helper that posts only to
the visible main window. No checkbox or 26/32 status changes in this diagnostic
update.

The authenticated V7 profile inventory is a separate runtime observation: receipt
`windows-v7-profile-inventory.json` has SHA-256
`108091f7f818f6e25ea31a2329e5391e26cc73ca368e65803110b83f722416c3`, with 155
host entries, 6 active Cyrene host entries and 4 disabled defaults. The default
agent preset is `cyrene-navigator`, with
`@cyrene/navigator-harness/compaction` as an active preset row. The Browser
delivery extension remains a separate client extension and is not counted as a
seventh host entry. The Linux upstream rebuild receipt
`linux-v7-upstream-rebuild-20260906.json` has SHA-256
`c970c0330cc1ac63c53c873b1642cf71e3465a42492fd666acbac169f8c66270` and records
exact Node `24.13.0`/pnpm `11.7.0` frozen install, official build exit 0 and
`corePatches: 0`; this is the scoped P0-02 build proof.

V7 authenticated profile inventory 是独立的 runtime 观察：
`windows-v7-profile-inventory.json` SHA-256 为
`108091f7f818f6e25ea31a2329e5391e26cc73ca368e65803110b83f722416c3`，包含 155 个 host
entries、6 个 active Cyrene host entries 和 4 个 disabled defaults。默认 agent preset 为
`cyrene-navigator`，其中 `@cyrene/navigator-harness/compaction` 是 active preset row。
Browser delivery extension 是独立 client extension，不计作第 7 个 host entry。Linux
upstream rebuild receipt `linux-v7-upstream-rebuild-20260906.json` SHA-256 为
`c970c0330cc1ac63c53c873b1642cf71e3465a42492fd666acbac169f8c66270`，记录 exact Node
`24.13.0`/pnpm `11.7.0` frozen install、official build exit 0 和 `corePatches: 0`；这是
P0-02 的 scoped build proof。

### Windows host receipt and quality slice / Windows 主机 receipt 与质量切片

The Windows host used the pinned DSH commit and a real SQLite persistence
service. The receipt/Profile/title checks were rerun without a model request:

- `input-receipts.integration.test.mjs`: 7/7 passed. The real SQLite case
  rejects a plugin-authored `rpcId` unless `source.kind` is `user`; the delayed
  read case propagates abort, closes the persistence handle and returns no
  partial result.
- `profile.integration.test.mjs`: 1/1 passed through the actual DSH Connection
  HTTP route. An authenticated cookie returned 200, a missing cookie returned
  401, and an untrusted Origin returned 403. The read did not append events or
  claim the writer; the provider tripwire observed zero model requests.
- `session-titles.test.mjs`: 5/5 passed. The existing Windows Python persistence
  quality result remains 17/17, and the Tauri quality result remains 17/17.
  The latest V7 client quality result is 23/23, including the local input
  put/remove error boundary from source `3b8ae97`.

The redacted host evidence is retained at
`C:\cyrene-nav-build\.task-cache\phase0-windows\windows-profile-receipt-title-evidence.json`;
its SHA-256 is
`e1f3df1f2d4ceb1c8de340cef9b372f49b57986bbe73f5567066b6e14022eaee`. It contains
runtime and result metadata only; credentials and raw session content are not
part of the record.

### Historical Windows V6 package boundary / 历史 Windows V6 安装包边界

Wrapper revision `2d4382a` completed the official build script. The resulting
`windows-v6-official-package.json` is explicitly marked
`intermediateSnapshot: true`, even though `buildComplete` is true and no skip
flags are present. Its SHA-256 is
`115b6ade230ee51a18a2acaf690d7bae00052be21bc781884522b0cf67155dad`. The two
recorded packages are `Cyrene Navigator_0.1.0_x64-setup.exe`
(`1aacf80b58827cc67ab8a0c27bc8411ac90e667bbc035e99e28d15f939eb105c`) and
`Cyrene Navigator_0.1.0_x64_en-US.msi`
(`8cb05ddb265880fce7458c4f1362799a066bc78b5919175e57b8b17b292e46a4`). Their
digests identify this intermediate snapshot only; they are not final package
or clean-machine evidence.

The strict client result `windows-v6-final-client-quality.json` has SHA-256
`8e2c3072dc2e8bce6392b4472f3cc32d3afbc1368dc6deda044ce75141479b06` and reports
22/22 tests, strict TypeScript, loader build, JavaScript syntax and whitespace
checks passing. The Tauri/native retained-input result
`windows-v6-retained-inputs-quality.json` has SHA-256
`b8e16e3cb0274786caae1260d89b91a2edffc55818e2a04b448b63875ba20002`; it reports
Rust 1.97.1 fmt/clippy and 17 Tauri tests passing, with
`installedGuiVerified: false`.

The intermediate installed-package native IPC records show put, idempotency,
conflict, bad-key rejection, persistence across reopen and idempotent removal:
`windows-v6-installed-native-ipc-before.json` has SHA-256
`b48680e449249386053306765c08632883b7d9fb1ab809785cabafd228a7b0fa`, and
`windows-v6-installed-native-ipc-after.json` has SHA-256
`d75f2102c1b8751f9956283f7b6ef4d1fadcd8dbedd38ae1cc826f574930334e`.
These are component/subscenario records. Final GUI failure recovery, complete
desktop recovery, reproducible package acceptance and clean Windows/VM first
launch remain pending, so no additional checkbox is changed.

The separate `windows-v6-receipt-outage-observed.json` record has SHA-256
`79c6c1155bc87a6186afa93d60707eff4af8d1da12006a6172662448f97ebf6f`. It
records the V6 UI mislabel in which a successful local write was displayed as
failed while the input was durably retained. V7's explicit local fault and
retry records above cover the corrected bounded behavior; the V6 result must
not be treated as a full desktop recovery PASS.

### Compaction compatibility boundary / Compaction 兼容性边界

Navigator revision `c09e737` passes the controlled four-test compaction suite
and a real CUDA summary stream. Its plugin removes auxiliary tools only from
summary requests; ordinary Agent requests retain their tools. The controlled
fixture demonstrates a tool-only summary response with the upstream behavior
and a text summary with this adapter. The original v5 provider stream was not
captured, so that historical failure cannot be attributed to a tool-only
response. This is a scoped compatibility result, not a new Phase 0 gate pass.

摘要插件只移除摘要请求中的辅助 tools，普通 Agent 的工具调用能力保持不变。受控
fixture 和真实 CUDA 摘要均通过；原 v5 provider 原始流未采样，不能把那次历史失败
归因为仅含 Tool Call 的响应。该结果不单独关闭 Phase 0 总门。

All records cited here are bounded, redacted metadata or hashes from the
Navigator `.navigator/proof` directory and
`C:\cyrene-nav-build\.task-cache`; this update intentionally excludes
credentials, launch secrets, prompts and raw conversation content.
