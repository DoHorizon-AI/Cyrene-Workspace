# Phase 0 evidence / Phase 0 验收证据

Recorded: 2026-09-06 03:30 UTC / 2026-09-05 America/New_York.
Status: partial local acceptance; adoption proof, hosted CI and canonical
acceptance remain pending.

状态：组件与真实进程边界已部分通过，完整 adoption proof、Windows 安装包、远程 CI
和合并验收仍待完成。下列 PASS 只覆盖明确列出的检查项。

## Exact upstream — P0-01 / 固定来源

- Repository: `https://github.com/deepseek-ai/deepseek-harness.git`
- Tag: `dsh-v0.1.3-alpha.1`
- Commit: `d347e703908d0406b7a7ef80e3a0e594d86b2215`
- Dependency manager: `pnpm@11.7.0`; Node `24.13.0`.
- Upstream `pnpm-lock.yaml` SHA-256: `2c903ab870f821ee2db62fa9417d11b1c2b9c65fbeec30e851ddc53c4cc8c383`.
- Upstream `package.json` SHA-256: `7177f05af3a57a4ba6ddb81ce28a686eeede647b9b1b9ccb9ef2fc0892e9c941`.
- Navigator `harness/upstream.lock.json` SHA-256: `71d8a33f2fdce27e1bf230161f1e6c4e89ed7ef1ea0bd9707a49d0f8e8a9b69c`.
- Bundle: `@cyrene/navigator-harness@0.1.0`.

Commands in Navigator:

```bash
node scripts/prepare-harness.mjs --root /tmp/cyrene-navigator-dsh-d347e703 --link
```

The bootstrap verified exact tag/HEAD, tracked and untracked source changes, and
lock hashes. Result: exit 0, `corePatches: 0`. A frozen install and upstream
`pnpm run build:official` completed on Linux. Windows reconstruction is separate
P0-02 evidence; no installer claim is made here. The selected prerelease was
built from pinned source because that exact package version was not on npm.

校验包括 tag、HEAD、已跟踪和未跟踪源码及锁文件摘要；成功退出且 Core patches 为 0。
Linux official build 已完成，Windows 的重建与安装验收继续单独验证。

## Persistence boundaries — P0-13, P0-14, P0-16 / 持久化边界

Implementation snapshot SHA-256 values in Navigator:

| File | SHA-256 |
| --- | --- |
| `harness/src/persistence.ts` | `77d5eab830c273b0aefebac304e27437c5c65320b38c423e617e1672668d4e79` |
| `harness/src/persistence-wire.ts` | `22468e1f2cf40ab3dd73deefa2fa46f2c4922b40a85f847aec5a3236b243b00c` |
| `src/cyrene_navigator/persistence/store.py` | `a10c45bde9a10db76b5f09b13f84ea69756935f5f6f563f4f92ddf6b52385b66` |
| `harness/tests/persistence.integration.test.mjs` | `b4f54f601fc6d8699e0f4f1502acb06ca5fc76ae07a5f6ae6487c2bef2a80d97` |
| `tests/test_persistence_process.py` | `a940e66a6a0da925731daf8614bce6b097434e9e07933d59808f6a665c42b7cd` |

Commands in Navigator:

```bash
uv run pytest -q
node /tmp/cyrene-navigator-dsh-d347e703/node_modules/typescript/bin/tsc -p harness/tsconfig.json
CYRENE_MANIFEST_BINARY=/home/baijin/Dev/Cyrene/Cyrene-Platform/target/debug/cy-manifest \
  node --test harness/tests/*.test.mjs
```

Results: Python 13 passed; strict TypeScript exit 0 without `skipLibCheck`; Node
8 passed, 0 failed, 0 cancelled, 0 skipped. The Node set includes one actual
upstream Session / HTTP service process test, four Codex import/continue tests
and three Rust stdio tests. Native Rust fmt/clippy and 18 tests passed on the
explicit `1.97.1` toolchain. A full Profile boot regression is being added.

The persistence test starts a real Uvicorn process and SQLite WAL store, writes
actual DSH v2 events through the production adapter, kills the service, restarts
it on the same database, reads the exact event prefix, and takes over the writer
with an observed epoch. The old writer's append and flush are rejected. API
tests additionally cover concurrent takeover, workspace isolation, batch replay,
conflicting batch content, gaps and expired leases. No GPU is required for these
storage-contract checks.

真实 Uvicorn、SQLite WAL 与上游 Session 接口已跨进程验证；重启恢复读取相同事件
前缀，显式 epoch 接管后旧 writer 被拒绝。数据库契约不需要 GPU。这些证据尚不等于
两个物理客户端设备完成产品 UI 操作，也不代表 OIDC 或组织级 RBAC 已完成。

Live Web observations: `cyrene-adoption-first` was created through the unchanged
DSH Web RPC, persisted in workspace `adoption-proof`, and listed/adopted again
after restarting the Web Profile. A Codex fixture import reached the same
backend through the authenticated Connection route. The import plugin now
releases its archive writer and exposes an explicit Continue action through
upstream AgentRegistry APIs. Desktop integration of that action remains pending,
so P0-27 stays unchecked.

Two independent Web Profile processes opened `cyrene-adoption-native`. The
second observed epoch 7, explicitly took over, and resumed through the unchanged
Session Controller. The persistence service was then killed and restarted;
all 45 stored events remained identical, including the original 20-event prefix
and its single Tool Call. This establishes two-process boundaries on one machine;
it is not evidence for two physical devices or cross-OS workspace-path mapping.

两个独立 Web Profile 完成 epoch 7 的显式接管；服务重启后 45 条事件一致，原有
20 条前缀及单个 Tool Call 保留。此处是同一机器的双进程证据，不能替代两台物理
设备或跨操作系统工作目录映射验收。

The subsequent continuation hit an in-progress old-CES/new-worker protocol
incompatibility while the real-time streaming path was being changed. Its
failure is retained in the same event log. Continuation acceptance will be
repeated after version-compatible streaming passes; it is not marked successful.

接管后的续聊遇到正在修改的旧 CES / 新 worker 协议不兼容；失败事件已保留。
流式兼容验证完成后重新验收，不把 accepted prompt 当作成功回答。

## Actual model Tool observation / 真实模型 Tool 往返

Before that streaming change, the real pinned Qwen model completed:

```text
Navigator Profile → Exchange → canonical resolver → CES
→ official model-api connector → vLLM
→ cyrene_manifest_id → Rust native host → existing cy-manifest
→ Tool Result → model continuation
```

Session: `cyrene-adoption-native`, preset `cyrene-navigator`.

| Model request | Provider input/output tokens | Result |
| --- | --- | --- |
| `chatcmpl-887043149e914c8780e858d8197e0623` | 5030 / 35 | Actual `cyrene_manifest_id` Tool Call |
| `chatcmpl-55d11bbada1e4da6879e0747cce434e8` | 5150 / 88 | Answer consumed the returned Artifact ID |

Tool correlation ID: `chatcmpl-tool-869edd69882750e4`.
Canonical manifest result:
`sha256:86198b4ac9e46d0f4d07f227a278e44aa8fec96796a7a9814a2a5f4db7e6d4b1`.
The result came from the real Rust utility; the model repeated that exact ID.

The first attempt with the upstream `standard` preset exceeded 8K context
because it loaded the developer's global Skill catalog and 27 tools. Navigator's
default preset now loads explicit Navigator Skill roots and a smaller upstream
tool composition. The original presets remain selectable for suitable models.

首轮已发现默认预设上下文过大并保留失败记录；Navigator 默认配置调整后，真实模型
完成了上述两次请求。后续观测到 token 片段集中到达，因此这里只确认内容与 Tool
往返，不把 SSE 格式保真当作实时 streaming 已通过。

## Real Codex archive observation / 真实 Codex 会话导入

An existing Codex `0.152.1` rollout from this Workspace, originally a short agent
configuration check, was imported through the authenticated Web route. No source
conversation content or raw archive is committed to this report.

- Original session: `01a0683d-be4e-7422-9562-a4ef3787d344`.
- Source bytes: 88,647; source SHA-256: `d690f168d5fca654fb18186a20ef58cbca1e4800dced4f170be1737e63aa778a`.
- Import session: `codex-d690f168d5fca654fb18186a20ef58cbca1e4800dced4f170be1737e63aa778a`.
- Converted messages: 4; historical tool calls/results: 2 / 2.
- Malformed records: 0; unknown content is retained with the raw archive/report.
- Developer/system records remain historical data; imported tools are non-executable.

来源标识、原文、摘要和转换报告保存在同一 Cyrene event log。实际桌面查看与
Continue 操作仍待验收，不能用 fixture 测试代替这些交互证据。

## Real CUDA environment / 真实 CUDA 环境

- OS: WSL2 Linux `6.18.33.2-microsoft-standard-WSL2`, x86-64.
- GPU: NVIDIA GeForce RTX 5070, 12,227 MiB, compute capability 12.0.
- Driver: `616.56`; PyTorch `2.11.0+cu130`; vLLM `0.25.1`.
- Python: managed CPython `3.12.11`, including development headers.
- Model: `Qwen/Qwen3-4B-Instruct-2507`.
- Revision: `cdbee75f17c01a7cc42f958dc650907174af0554`.
- Model alias: `cyrene-proof-text`; BF16; context 8,192; max concurrent sequences 1.

The environment was installed from Reactor's existing frozen `uv.lock` in an
owned temporary virtual environment. CUDA matrix multiplication succeeded.
vLLM loaded the actual model and `/health` returned 200. Compatibility settings:

```text
VLLM_HOST_IP=127.0.0.1
GLOO_SOCKET_IFNAME=lo
NCCL_SOCKET_IFNAME=lo
VLLM_USE_V2_MODEL_RUNNER=0
VLLM_USE_FLASHINFER_SAMPLER=0
--gpu-memory-utilization 0.9 --enforce-eager
--enable-auto-tool-choice --tool-call-parser hermes
```

Earlier attempts failed on WSL UVA support, distributed interface selection,
sampler compilation and missing Python headers. Supported runtime configuration
and a managed interpreter resolved those failures without an engine source patch.

这是 Phase 0 外部 Provider 的真实环境证据；进程尚未通过 Reactor/Platform 产品部署
启动，因此不能勾选 Phase 1 的部署、Placement 或 ready 验收。

## Remaining acceptance / 尚待验收

- Repeat the real model Tool path after the streaming transport and compatibility tests pass.
- Complete client restart, explicit takeover UI and non-executable historical replay proof.
- Desktop viewing and explicit Continue for the imported real Codex archive.
- Windows frozen build, installer launch, chat/tool and restart without a source development environment.
- Hosted checks and post-merge read-back; current task branches are not canonical acceptance.

Credentials and private launch URLs are deliberately absent from this report.
凭据与私有启动 URL 不进入验收文档。
