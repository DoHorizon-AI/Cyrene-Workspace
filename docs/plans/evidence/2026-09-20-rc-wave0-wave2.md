# Evidence — First RC Wave 0 / Wave 2 (2026-09-20)

> Status: LOCAL_TEST_PASS for the listed commands. No ACTUAL_PASS is claimed.
> 状态：以下命令为 LOCAL_TEST_PASS；不声明任何 ACTUAL_PASS。

## Environment / 环境

| Field | Value |
| --- | --- |
| OS | Linux (WSL2 development profile), kernel-backed ext4 worktrees |
| Python | 3.12.3 |
| GPU | NVIDIA GeForce RTX 5070, 12227 MiB, driver 616.92 |
| CUDA toolkit | not installed (`nvcc` absent) |
| vLLM / LLaMA Factory | not installed on this host |
| Hosted CI | NOT_RUN (no push performed in this session) |

The WSL2 host is a development validation environment. Native Ubuntu 24.04
NVIDIA acceptance has not been executed.

## Repository state / 仓库状态

The changes were committed on `develop` in `Cyrene-Plugins-Official`
(`31147dd264de527eee6ad27882f3abe1718081fe`), `Cyrene-Reactor`
(`44c5113ba246ab8894dea600ff95058d05c11e04`), `Cyrene-Exchange`
(`7e5d132334f940b9e22450a489798fa1644b5772`), and `Cyrene-Workspace`. Nothing
was pushed; hosted CI has not consumed these revisions, and the Workspace
plugin package pins stay on the previous published Plugins revision until the
new revision is pushed.

## Evidence records / 证据记录

### 1. Reactor persisted ModelImport — RC-W2-01

- Command: `cd Cyrene-Services/Cyrene-Reactor/product && uv run --no-sync pytest -q tests`
- Result: `19 passed` (includes `tests/test_model_import.py`: lifecycle, pinned
  revision enforcement, remote-code refusal, traversal refusal, persisted
  failure, idempotent replay, frozen-path parity).
- Static: `ruff check src tests`, `ruff format --check src tests`, `mypy` all
  pass; `openapi-spec-validator ../contracts/product/v1/openapi.yaml` → `OK`.
- Limitations: the import is validated through the injected test port; no
  binding downloaded or hashed real model weights in this record.

### 2. vLLM runtime model import — RC-W2-02

- Command: `cd Cyrene-Plugins-Official && PYTHONPATH=plugins/serving/vllm-runtime python -m pytest -q plugins/serving/vllm-runtime/tests contracts/tck/execution-engine-v1`
- Result: `9 passed` (local directory validation, artifact publication into the
  real local CAS, stable manifest digest, private credential ref resolution,
  remote-code refusal, incomplete-model refusal, stand-in lifecycle, TCK schema
  and release checks).
- Limitations: A real `vllm serve` process, CUDA inference, and GPU release are
  not exercised. The TCK operator-binding probe was not configured.

### 3. LLaMA Factory capability records — RC-W0-02

- Command: `cd Cyrene-Plugins-Official && PYTHONPATH=plugins/training/llama-factory python -m pytest -q plugins/training/llama-factory/tests contracts/tck/training-llama-factory-v1`
- Result: `7 passed` (real run-directory compile, device/identity authority
  refusal, DirectPluginRuntime dispatch, TCK schema and authority checks).
- Static: `ruff check plugins/serving/vllm-runtime plugins/training/llama-factory`
  → `All checks passed!`.
- Limitations: no real CUDA training; no checkpoint or adapter production.

### 4. Plugins packaging and catalog — RC-W0-02

- Commands:
  - `python3 tools/ci/validate_manifests.py --root .` → `MANIFEST_SCHEMA: PASS manifests=13`
  - `python3 tools/ci/capability_catalog.py --root . --check` → `CAPABILITY_CATALOG: PASS capabilities=14 implementations=20`
  - `python3 tools/ci/update_source_manifest.py --root .` then
    `python3 tools/ci/verify_source_manifest.py --root . --allow-git-metadata`
    → `SOURCE_MANIFEST: PASS ... files=650 protected=3`
  - `python3 tools/ci/check_public_repository.py` → `PUBLIC_HYGIENE: PASS`
- New records: `execution.engine.v1` manifest/schema/TCK/verification entry,
  provenance files for both engines, TCK suites under `contracts/tck/`, and
  public-CI install/lint/test/TCK coverage.
- Limitations: the vLLM verification level is `IMPLEMENTATION_VERIFIED` with
  `execution_mode: SIMULATED`; it must not be read as a live engine claim.

### 5. Exchange API keys, lists, and `/v1` scope — RC-W2-04..06

- Command: `cd Cyrene-Services/Cyrene-Exchange/product && PYTHONPATH=../src:src uv run --no-sync pytest -q -p no:cacheprovider`
- Result: `33 passed` (secret shown once, digest-only replay without secret,
  expiry validation, workspace isolation, revoke, `/v1/models` filtering,
  `model_not_permitted` rejection with audit, endpoint/route lists, CLI
  `key issue/list/revoke`, existing gateway lifecycle).
- Core: `uv run --no-sync pytest -q tests/test_canonical_runtime.py tests/test_billing_client.py` → `14 passed`.
  `tests/test_direct_plugin.py`, `tests/test_platform_integration.py`, and
  `tests/test_platform_resolver.py` fail collection locally because the
  `cyrene_model_provider_contracts` package is not installed in this venv; that
  is a pre-existing environment gap, not a regression from this change.
- Static: root `ruff check src tests integration_tests` passes; product
  `ruff check`, `ruff format --check`, `mypy` pass;
  `openapi-spec-validator contracts/product/v1/openapi.yaml` → `OK`.
- Limitations: no external client (Open WebUI, Cherry Studio, Continue) was
  exercised; no remote HTTPS pairing.

### 6. Workspace baseline and release lock — RC-W0-03..05

- Command: `cd Cyrene-Workspace && .venv/bin/python -m pytest -q tests/test_release_lock.py tests/test_cyrene_dev.py tests/test_reference_runtime.py tests/test_cross_repo_policy.py`
- Result: `16 passed`.
- `python scripts/validate_release_lock.py --require-terminal` → `VALID` is
  expected only after the engine blockers clear; currently `BLOCKED` with
  `training.llama-factory.v1 is PENDING_REAL_ACCEPTANCE` and
  `execution.engine.v1 is EVIDENCE_SCOPED`, exit code 1 by design.
- Limitations: no `cyrene-dev up` run was executed in this session; the lock
  repository revisions record the new local `develop` commits, while the
  Workspace plugin package pins remain on the published Plugins revision until
  the new revision is pushed.

## Module boundaries re-affirmed / 边界复核

- Reactor owns ModelImport metadata and validation evidence; the Plugins vLLM
  runtime owns model materialization, artifact publication, and process lifetime.
- Exchange owns API keys, routes, and audit; the Plugins/Platform boundary owns
  provider execution.
- No Product state was copied into a browser cache or a second authority.

## Not run / 未运行

- Native Ubuntu 24.04 package install, `cyrene` CLI, pairing, HTTPS.
- Real vLLM launch, GPU load/probe/release, and real inference.
- Real LLaMA Factory CUDA training, checkpoint, cancel, resume.
- Catalyst CSV/Parquet import, split seed, and the Yield event/SSE/resume APIs.
- WebUI pages, throughput and accessibility tests, external client matrix.
- Hosted CI for the exact revision (no push).